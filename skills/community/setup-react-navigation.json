{
  "id": "community.setup-react-navigation.v1",
  "keywords": [
    "react navigation",
    "setup react navigation",
    "mobile navigation",
    "stack navigator",
    "tab navigation",
    "expo navigation",
    "drawer navigator",
    "react native navigation",
    "add navigation",
    "bottom tabs"
  ],
  "contexts": [
    "react-native",
    "expo",
    "typescript"
  ],
  "title": "Set up React Navigation for Expo and React Native",
  "summary": "Install and configure React Navigation v7 with a Stack navigator, Bottom Tabs, and TypeScript-safe route params in an Expo project.",
  "source": "community",
  "declaration": "I will set up React Navigation v7 in your Expo/React Native project with Stack and Tab navigators, TypeScript route types, and deep linking ready to go.",
  "instructions": "## Set up React Navigation for Expo and React Native\n\n### 1. Install core dependencies\n- React Navigation is split into packages — install the core and the native helpers:\n```bash\nnpx expo install @react-navigation/native react-native-screens react-native-safe-area-context\n```\n- IMPORTANT: Use `npx expo install` (NOT `npm install`) so Expo picks the compatible version for your SDK.\n- `react-native-screens` provides native stack screens for better performance.\n- `react-native-safe-area-context` handles the notch and home indicator on modern devices.\n\n### 2. Choose and install a navigator\n- Install the navigator types you need:\n```bash\n# Stack navigator (most common — push/pop screens)\nnpx expo install @react-navigation/native-stack\n\n# Bottom tab bar\nnpx expo install @react-navigation/bottom-tabs\n\n# Material top tabs (horizontal swipe)\nnpx expo install @react-navigation/material-top-tabs react-native-tab-view\n\n# Drawer (side menu)\nnpx expo install @react-navigation/drawer react-native-gesture-handler react-native-reanimated\n```\n- For most apps, start with `native-stack` + `bottom-tabs`.\n\n### 3. Wrap the app in NavigationContainer\n- Wrap your root component in `<NavigationContainer>` in `App.tsx`:\n```tsx\nimport { NavigationContainer } from '@react-navigation/native';\n\nexport default function App() {\n  return (\n    <NavigationContainer>\n      <RootNavigator />\n    </NavigationContainer>\n  );\n}\n```\n- There must be exactly ONE `<NavigationContainer>` at the root of your app.\n\n### 4. Define TypeScript route params\n- Declare param types for all screens to get full type safety:\n```ts\n// src/types/navigation.ts\nexport type RootStackParamList = {\n  Home: undefined;\n  Profile: { userId: string };\n  Settings: undefined;\n};\n\nexport type TabParamList = {\n  Feed: undefined;\n  Search: undefined;\n  Profile: { userId: string };\n};\n```\n- Then create typed hooks (do this once and reuse):\n```ts\n// src/types/navigation.ts (add to the same file)\nimport { NativeStackNavigationProp } from '@react-navigation/native-stack';\nimport { RouteProp } from '@react-navigation/native';\n\nexport type RootStackNavProp<T extends keyof RootStackParamList> =\n  NativeStackNavigationProp<RootStackParamList, T>;\n\nexport type RootStackRouteProp<T extends keyof RootStackParamList> =\n  RouteProp<RootStackParamList, T>;\n```\n\n### 5. Create a Stack navigator\n```tsx\n// src/navigation/RootNavigator.tsx\nimport { createNativeStackNavigator } from '@react-navigation/native-stack';\nimport { RootStackParamList } from '../types/navigation';\nimport HomeScreen from '../screens/HomeScreen';\nimport ProfileScreen from '../screens/ProfileScreen';\n\nconst Stack = createNativeStackNavigator<RootStackParamList>();\n\nexport function RootNavigator() {\n  return (\n    <Stack.Navigator\n      initialRouteName=\"Home\"\n      screenOptions={{\n        headerStyle: { backgroundColor: '#fff' },\n        headerTintColor: '#000',\n      }}\n    >\n      <Stack.Screen name=\"Home\" component={HomeScreen} />\n      <Stack.Screen\n        name=\"Profile\"\n        component={ProfileScreen}\n        options={{ title: 'User Profile' }}\n      />\n    </Stack.Navigator>\n  );\n}\n```\n\n### 6. Navigate between screens\n- In a screen component, access the typed `navigation` and `route` props:\n```tsx\n// src/screens/HomeScreen.tsx\nimport { useNavigation } from '@react-navigation/native';\nimport { RootStackNavProp } from '../types/navigation';\n\nexport default function HomeScreen() {\n  const navigation = useNavigation<RootStackNavProp<'Home'>>();\n\n  return (\n    <Button\n      title=\"Go to Profile\"\n      onPress={() => navigation.navigate('Profile', { userId: '123' })}\n    />\n  );\n}\n```\n- Access route params with `useRoute()`:\n```tsx\n// src/screens/ProfileScreen.tsx\nimport { useRoute } from '@react-navigation/native';\nimport { RootStackRouteProp } from '../types/navigation';\n\nexport default function ProfileScreen() {\n  const route = useRoute<RootStackRouteProp<'Profile'>>();\n  const { userId } = route.params; // fully typed\n  return <Text>User ID: {userId}</Text>;\n}\n```\n\n### 7. Add a Bottom Tab bar\n```tsx\n// src/navigation/TabNavigator.tsx\nimport { createBottomTabNavigator } from '@react-navigation/bottom-tabs';\nimport { Ionicons } from '@expo/vector-icons';\nimport { TabParamList } from '../types/navigation';\nimport FeedScreen from '../screens/FeedScreen';\nimport SearchScreen from '../screens/SearchScreen';\n\nconst Tab = createBottomTabNavigator<TabParamList>();\n\nexport function TabNavigator() {\n  return (\n    <Tab.Navigator\n      screenOptions={({ route }) => ({\n        tabBarIcon: ({ color, size }) => {\n          const iconName = route.name === 'Feed' ? 'home' : 'search';\n          return <Ionicons name={iconName} size={size} color={color} />;\n        },\n        tabBarActiveTintColor: '#6366f1',\n        tabBarInactiveTintColor: 'gray',\n      })}\n    >\n      <Tab.Screen name=\"Feed\" component={FeedScreen} />\n      <Tab.Screen name=\"Search\" component={SearchScreen} />\n    </Tab.Navigator>\n  );\n}\n```\n- To nest tabs inside a stack, use `TabNavigator` as the component for one of the stack screens.\n\n### 8. Nest navigators (Stack inside Tabs — common pattern)\n```tsx\n// App structure: Tabs at root, each tab has its own Stack\nexport function RootNavigator() {\n  return (\n    <Stack.Navigator screenOptions={{ headerShown: false }}>\n      <Stack.Screen name=\"Tabs\" component={TabNavigator} />\n      <Stack.Screen name=\"Settings\" component={SettingsScreen} />\n    </Stack.Navigator>\n  );\n}\n```\n- IMPORTANT: When nesting, set `headerShown: false` on the outer navigator's screen to avoid double headers.\n\n### 9. Configure deep linking\n- Add a `linking` config to `<NavigationContainer>` for universal links and custom schemes:\n```tsx\nconst linking = {\n  prefixes: ['myapp://', 'https://myapp.com'],\n  config: {\n    screens: {\n      Home: '',\n      Profile: 'user/:userId',\n      Settings: 'settings',\n    },\n  },\n};\n\n<NavigationContainer linking={linking}>\n  <RootNavigator />\n</NavigationContainer>\n```\n- Then in `app.json` (Expo), add the scheme:\n```json\n{\n  \"expo\": {\n    \"scheme\": \"myapp\"\n  }\n}\n```\n\n### Common errors & fixes\n- **`Error: couldn't find a navigation object`** — You called `useNavigation()` in a component that is not a child of `<NavigationContainer>`. Move the component into the navigator tree.\n- **`The action NAVIGATE with payload was not handled`** — The target screen name doesn't exist in the navigator. Check for typos between the `Stack.Screen name` and `navigation.navigate('ScreenName')`.\n- **Blank screen / no transition animation** — You likely forgot to install `react-native-screens` or didn't run `npx expo install`. Run `npx expo install react-native-screens` and restart the Metro bundler.\n- **`requireNativeComponent: \"RNCSafeAreaView\" was not found`** — `react-native-safe-area-context` needs a native module rebuild. Run `npx expo prebuild` or ensure you're using an Expo dev client build.\n- **Drawer swipe gesture not working** — For the drawer navigator, you must import and call `import 'react-native-gesture-handler'` at the very TOP of your `App.tsx` entry file, before any other imports.\n- **TypeScript: `Argument of type 'string' is not assignable to parameter`** — Your param list types are mismatched. Ensure the `ParamList` type for `navigate()` matches the `createNativeStackNavigator<YourParamList>()` generic.\n- **Back button doesn't dismiss modal** — Use `presentation: 'modal'` in the screen options and `navigation.goBack()` rather than `navigation.navigate()` to dismiss.\n\n### What NOT to do\n- Do NOT use `npm install` for React Navigation packages in an Expo project — use `npx expo install` to get SDK-compatible versions. Mismatched versions cause cryptic native errors.\n- Do NOT put `<NavigationContainer>` inside a screen component — there must be exactly one at the root.\n- Do NOT use `navigation.push()` and `navigation.navigate()` interchangeably — `navigate()` goes to an existing screen instance or creates one; `push()` always creates a new instance on the stack.\n- Do NOT import `@react-navigation/drawer` without also installing `react-native-gesture-handler` and `react-native-reanimated` — the drawer requires native gesture support.\n- Do NOT access `navigation` or `route` in components that aren't directly rendered by a `Stack.Screen` or `Tab.Screen` — use `useNavigation()` / `useRoute()` hooks for components deeper in the tree.\n- Do NOT pass navigation props down through multiple component layers — use the `useNavigation()` hook at any level inside the navigator tree instead.\n- Do NOT define new param list types inside component files — keep them in a shared `src/types/navigation.ts` file so all screens can import the same source of truth.",
  "version": "1.0.0",
  "updated_at": "2026-06-27T00:00:00Z",
  "category": "mobile"
}
