{
  "id": "community.setup-turso-drizzle.v1",
  "keywords": ["turso", "libsql", "setup turso", "turso database", "turso drizzle", "edge database", "sqlite edge", "libsql drizzle", "turso setup", "turso nextjs"],
  "contexts": ["nextjs", "typescript", "nodejs"],
  "title": "Set Up Turso (LibSQL) with Drizzle ORM",
  "summary": "Configure Turso edge-compatible SQLite database with Drizzle ORM for serverless and edge deployments.",
  "source": "community",
  "declaration": "I'll set up Turso database with Drizzle ORM, including schema definition, migrations, and edge-compatible client configuration.",
  "instructions": "## Set Up Turso with Drizzle ORM\n\n### 1. Install Dependencies\n\n```bash\nnpm install drizzle-orm @libsql/client\nnpm install -D drizzle-kit\n```\n\n### 2. Create a Turso Database\n\n```bash\n# Install Turso CLI\ncurl -sSfL https://get.tur.so/install.sh | bash\n\n# Log in\nturso auth login\n\n# Create a database\nturso db create my-app\n\n# Get the connection URL\nturso db show my-app --url\n\n# Create an auth token\nturso db tokens create my-app\n```\n\n### 3. Configure Environment Variables\n\nAdd to `.env.local`:\n\n```\nTURSO_DATABASE_URL=libsql://your-db.turso.io\nTURSO_AUTH_TOKEN=your-auth-token\n```\n\n### 4. Create the Drizzle Client\n\nCreate `src/db/index.ts`:\n\n```typescript\nimport { drizzle } from 'drizzle-orm/libsql';\nimport { createClient } from '@libsql/client';\n\nconst client = createClient({\n  url: process.env.TURSO_DATABASE_URL!,\n  authToken: process.env.TURSO_AUTH_TOKEN,\n});\n\nexport const db = drizzle(client);\n```\n\n### 5. Define Schema\n\nCreate `src/db/schema.ts`:\n\n```typescript\nimport { text, integer, sqliteTable } from 'drizzle-orm/sqlite-core';\n\nexport const users = sqliteTable('users', {\n  id: integer('id').primaryKey({ autoIncrement: true }),\n  email: text('email').notNull().unique(),\n  name: text('name'),\n  createdAt: text('created_at').$defaultFn(() => new Date().toISOString()),\n});\n\nexport type User = typeof users.$inferSelect;\nexport type NewUser = typeof users.$inferInsert;\n```\n\n### 6. Configure Drizzle Kit\n\nCreate `drizzle.config.ts`:\n\n```typescript\nimport type { Config } from 'drizzle-kit';\n\nexport default {\n  schema: './src/db/schema.ts',\n  out: './drizzle',\n  dialect: 'turso',\n  dbCredentials: {\n    url: process.env.TURSO_DATABASE_URL!,\n    authToken: process.env.TURSO_AUTH_TOKEN,\n  },\n} satisfies Config;\n```\n\n### 7. Add Migration Scripts to package.json\n\n```json\n{\n  \"scripts\": {\n    \"db:generate\": \"drizzle-kit generate\",\n    \"db:migrate\": \"drizzle-kit migrate\",\n    \"db:studio\": \"drizzle-kit studio\"\n  }\n}\n```\n\n### 8. Run First Migration\n\n```bash\nnpm run db:generate\nnpm run db:migrate\n```\n\n### 9. Query in Server Components or Route Handlers\n\n```typescript\nimport { db } from '@/db';\nimport { users } from '@/db/schema';\nimport { eq } from 'drizzle-orm';\n\n// Insert\nconst [user] = await db.insert(users).values({\n  email: 'alice@example.com',\n  name: 'Alice',\n}).returning();\n\n// Select\nconst all = await db.select().from(users);\n\n// Where\nconst found = await db.select().from(users).where(eq(users.email, 'alice@example.com'));\n```\n\n### 10. Edge Runtime — Use HTTP Transport\n\nFor Cloudflare Workers or Next.js Edge routes, swap to the HTTP client:\n\n```typescript\nimport { drizzle } from 'drizzle-orm/libsql';\nimport { createClient } from '@libsql/client/http';\n\nconst client = createClient({\n  url: process.env.TURSO_DATABASE_URL!,\n  authToken: process.env.TURSO_AUTH_TOKEN,\n});\n\nexport const db = drizzle(client);\n```\n\n## Common Errors\n\n**`TURSO_DATABASE_URL must start with libsql://`**\n- Remote URLs use `libsql://`, not `https://`\n- For local dev without Turso, use `file:local.db` (no `authToken` needed)\n\n**`authToken is required for remote databases`**\n- Remote Turso databases always need a token\n- Generate one: `turso db tokens create <db-name>`\n\n**`Cannot use WebSocket in Edge Runtime`**\n- Import from `@libsql/client/http` instead of `@libsql/client`\n- WebSocket transport is unavailable in Cloudflare Workers and Next.js Edge Runtime\n\n**`dialect 'turso' not recognized` in drizzle-kit**\n- Requires `drizzle-kit >= 0.21.0` — run `npm install -D drizzle-kit@latest`\n\n**Type error on `.$defaultFn`**\n- Use `.$defaultFn(() => new Date().toISOString())` for computed defaults, not `.default(expression)`\n\n## What NOT to Do\n\n- **Do NOT use `pg` or `mysql2` drivers** — Turso is SQLite dialect (`drizzle-orm/libsql`)\n- **Do NOT mix Postgres column types** (`pgTable`, `serial`) with a Turso schema — use `sqliteTable`\n- **Do NOT hardcode credentials** — always read from environment variables\n- **Do NOT run migrations inside Edge Runtime** — use `drizzle-kit migrate` from a Node.js context only\n- **Do NOT use `integer` for UUID primary keys** — use `text` with `.$defaultFn(() => crypto.randomUUID())`\n- **Do NOT share one `createClient()` call across serverless invocations** — create a new client per request or use a module-level singleton with care",
  "version": "1.0.0",
  "updated_at": "2026-06-07T00:00:00Z"
}
