{
  "id": "community.setup-msw.v1",
  "keywords": [
    "msw",
    "mock service worker",
    "api mocking",
    "mock api",
    "setup msw",
    "add msw",
    "mock fetch",
    "testing mocks",
    "intercept requests"
  ],
  "contexts": [
    "react",
    "nextjs",
    "typescript",
    "testing",
    "vitest",
    "storybook"
  ],
  "title": "Set up MSW (Mock Service Worker) for API mocking",
  "summary": "Full MSW v2 integration — request handlers, Node.js test server for Vitest, browser service worker for dev, and Storybook addon.",
  "source": "community",
  "declaration": "I will set up MSW (Mock Service Worker) v2 for this project. MSW intercepts fetch and XHR at the network layer — no real requests leave the browser or test process. This integrates with Vitest for unit/integration tests, with browser devtools for local development, and with Storybook for component stories. If you need to mock at the module level instead of the network level, say so.",
  "instructions": "## Set up MSW v2 (Mock Service Worker)\n\n### 0. Prerequisites\n- MSW v2 requires Node.js >= 18 and a modern browser.\n- The project should already have Vitest, Jest, or Storybook configured, OR you want to intercept real API calls during local development.\n- MSW is a **devDependency** — it should never run in production.\n\n### 1. Install MSW\n```bash\nnpm install msw --save-dev\n```\n- `msw` ships both browser (service worker) and Node.js server in a single package.\n\n### 2. Create request handlers\nCreate `src/mocks/handlers.ts`:\n```typescript\nimport { http, HttpResponse } from 'msw';\n\nexport const handlers = [\n  // Mock a GET endpoint\n  http.get('/api/users', () => {\n    return HttpResponse.json([\n      { id: '1', name: 'Alice', email: 'alice@example.com' },\n      { id: '2', name: 'Bob', email: 'bob@example.com' },\n    ]);\n  }),\n\n  // Mock a GET with URL params\n  http.get('/api/users/:id', ({ params }) => {\n    const { id } = params;\n    return HttpResponse.json({ id, name: 'User ' + id });\n  }),\n\n  // Mock a POST with request body\n  http.post('/api/users', async ({ request }) => {\n    const body = await request.json() as { name: string; email: string };\n    return HttpResponse.json({ id: '3', ...body }, { status: 201 });\n  }),\n\n  // Mock an error response\n  http.delete('/api/users/:id', ({ params }) => {\n    if (params.id === '999') {\n      return new HttpResponse(null, { status: 404 });\n    }\n    return new HttpResponse(null, { status: 204 });\n  }),\n];\n```\nKey MSW v2 API differences from v1:\n- Use `http` (not `rest`) — import from `msw`.\n- Use `HttpResponse.json(data)` instead of `res(ctx.json(data))`.\n- Use `new HttpResponse(null, { status: 404 })` instead of `res(ctx.status(404))`.\n- The `res`, `ctx`, `req` compose API from v1 no longer exists.\n\n### 3. Node.js server for tests (Vitest)\nCreate `src/mocks/node.ts`:\n```typescript\nimport { setupServer } from 'msw/node';\nimport { handlers } from './handlers';\n\nexport const server = setupServer(...handlers);\n```\n- `msw/node` uses Node.js `http.ClientRequest` interception via the `@mswjs/interceptors` package. It does NOT use a service worker.\n- The server intercepts all outgoing HTTP requests from the test process.\n\n### 4. Vitest setup file\nCreate `src/mocks/setup.ts` (or add to an existing setup file):\n```typescript\nimport { afterAll, afterEach, beforeAll } from 'vitest';\nimport { server } from './node';\n\n// Start the server before all tests\nbeforeAll(() => server.listen({ onUnhandledRequest: 'warn' }));\n\n// Reset any runtime request overrides after each test\nafterEach(() => server.resetHandlers());\n\n// Clean up after all tests\nafterAll(() => server.close());\n```\n\nRegister the setup file in `vitest.config.ts`:\n```typescript\nimport { defineConfig } from 'vitest/config';\n\nexport default defineConfig({\n  test: {\n    setupFiles: ['./src/mocks/setup.ts'],\n    environment: 'jsdom', // required for component tests\n  },\n});\n```\n\n### 5. Write tests that use the mock\n```typescript\nimport { render, screen, waitFor } from '@testing-library/react';\nimport { describe, expect, it } from 'vitest';\nimport { http, HttpResponse } from 'msw';\nimport { server } from '../mocks/node';\nimport { UserList } from './UserList';\n\ndescribe('UserList', () => {\n  it('renders users from API', async () => {\n    render(<UserList />);\n    // Handlers in src/mocks/handlers.ts intercept GET /api/users automatically\n    await waitFor(() => {\n      expect(screen.getByText('Alice')).toBeInTheDocument();\n      expect(screen.getByText('Bob')).toBeInTheDocument();\n    });\n  });\n\n  it('shows error state when API fails', async () => {\n    // Override the handler for this test only\n    server.use(\n      http.get('/api/users', () => {\n        return new HttpResponse(null, { status: 500 });\n      })\n    );\n\n    render(<UserList />);\n    await waitFor(() => {\n      expect(screen.getByText('Failed to load users')).toBeInTheDocument();\n    });\n    // After this test, afterEach calls server.resetHandlers() — the override is removed\n  });\n});\n```\n- `server.use(...)` inside a test overrides specific handlers for that test only.\n- `afterEach(() => server.resetHandlers())` ensures overrides don't leak between tests.\n- Always `await waitFor(...)` — async fetch calls complete after the initial render.\n\n### 6. Initialize the browser service worker\nRun once to generate the service worker file:\n```bash\nnpx msw init public/ --save\n```\n- This creates `public/mockServiceWorker.js`.\n- The `--save` flag adds a `msw` entry to `package.json` so the command reruns on `npm install`.\n- Commit `public/mockServiceWorker.js` to version control (it must be served at the same origin as your app).\n- If your project's static files directory is NOT `public/`, replace it with the correct path (e.g., `static/` for CRA, `dist/` for Vite — check your framework docs).\n\n### 7. Browser worker setup\nCreate `src/mocks/browser.ts`:\n```typescript\nimport { setupWorker } from 'msw/browser';\nimport { handlers } from './handlers';\n\nexport const worker = setupWorker(...handlers);\n```\n\nStart the worker in your app entry point (`src/main.tsx` or `src/index.tsx`):\n```typescript\nasync function enableMocking() {\n  if (process.env.NODE_ENV !== 'development') return;\n  const { worker } = await import('./mocks/browser');\n  return worker.start({\n    onUnhandledRequest: 'bypass', // let unhandled requests pass through\n  });\n}\n\nenableMocking().then(() => {\n  // Start React after MSW is ready\n  createRoot(document.getElementById('root')!).render(<App />);\n});\n```\n\nFor Next.js App Router, add to a client component in your root layout:\n```typescript\n'use client';\nimport { useEffect } from 'react';\n\nexport function MSWProvider({ children }: { children: React.ReactNode }) {\n  useEffect(() => {\n    if (process.env.NODE_ENV === 'development') {\n      import('./mocks/browser').then(({ worker }) => {\n        worker.start({ onUnhandledRequest: 'bypass' });\n      });\n    }\n  }, []);\n  return <>{children}</>;\n}\n```\n\n### 8. Storybook integration\nInstall the addon:\n```bash\nnpm install msw-storybook-addon --save-dev\n```\nIn `.storybook/preview.ts`:\n```typescript\nimport { initialize, mswLoader } from 'msw-storybook-addon';\n\ninitialize({ onUnhandledRequest: 'bypass' });\n\nexport default {\n  loaders: [mswLoader],\n  parameters: {\n    msw: {\n      handlers: [],\n    },\n  },\n};\n```\n\nIn a story file:\n```typescript\nimport type { Meta, StoryObj } from '@storybook/react';\nimport { http, HttpResponse } from 'msw';\nimport { UserProfile } from './UserProfile';\n\nexport default { component: UserProfile } satisfies Meta;\n\nexport const LoggedIn: StoryObj = {\n  parameters: {\n    msw: {\n      handlers: [\n        http.get('/api/me', () =>\n          HttpResponse.json({ id: '1', name: 'Alice', role: 'admin' })\n        ),\n      ],\n    },\n  },\n};\n\nexport const Unauthorized: StoryObj = {\n  parameters: {\n    msw: {\n      handlers: [\n        http.get('/api/me', () => new HttpResponse(null, { status: 401 })),\n      ],\n    },\n  },\n};\n```\n\n### 9. Verify\n- **Vitest**: Run `npx vitest`. Tests that call fetch should use the mocked responses without network requests. No `[MSW] Warning: intercepted an unhandled request` means handlers are matching.\n- **Browser**: Start dev server. Open browser DevTools → Console. You should see `[MSW] Mocking enabled.`. Check Network tab — mocked requests return instantly with no actual outgoing call.\n- **Storybook**: Run Storybook. Stories with `parameters.msw.handlers` should show different UI states based on the mock data.\n\n### Common errors & fixes\n- **`TypeError: worker.start is not a function`** → You imported `setupServer` from `msw/node` in browser code, or `setupWorker` from `msw/browser` in test code. `msw/browser` is for the browser; `msw/node` is for Node.js/Vitest.\n- **`Failed to register a ServiceWorker`** → The `mockServiceWorker.js` file is missing from `public/`. Run `npx msw init public/ --save`.\n- **Handlers not intercepting requests** → The request URL in the handler doesn't match the actual fetch URL. For relative URLs like `fetch('/api/users')`, use `http.get('/api/users', ...)`. For absolute URLs, use the full URL: `http.get('https://api.example.com/users', ...)`.\n- **Tests still making real network requests** → The MSW server was not started. Verify `vitest.config.ts` has the correct path in `setupFiles` and that `server.listen()` is called in `beforeAll`.\n- **`[MSW] Warning: intercepted an unhandled GET /api/something`** → Add a handler for this route, or set `onUnhandledRequest: 'bypass'` to silently pass through unmatched requests.\n- **MSW v1 imports not working** → You're using v1 syntax (`rest.get`, `res(ctx.json(...))`) with a v2 installation. Replace `rest` with `http`, and replace `res(ctx.json(data))` with `HttpResponse.json(data)`.\n- **`afterEach` reset not working** → Verify `server.resetHandlers()` is called in `afterEach`, not `afterAll`. `afterAll` only runs once at the end of the file.\n- **Storybook MSW not mocking** → Confirm `mswLoader` is in the `loaders` array in `.storybook/preview.ts` and the service worker file is in the Storybook static folder.\n\n### What NOT to do\n- Do NOT start the MSW worker in production. Guard all `worker.start()` calls with `if (process.env.NODE_ENV !== 'development')`. MSW should never intercept requests for real users.\n- Do NOT use MSW v1 syntax (`rest`, `ctx`, `req`, `res`) with a v2 installation. The entire response composition API was redesigned. Replace with `http` and `HttpResponse`.\n- Do NOT mock at the module level with `vi.mock('axios')` or `vi.mock('./apiClient')` when MSW is available. MSW intercepts at the network layer, which tests more of the real code path (including fetch wrappers, retry logic, auth headers).\n- Do NOT skip `server.resetHandlers()` in `afterEach`. Runtime handler overrides added with `server.use(...)` inside tests will leak into subsequent tests if not reset.\n- Do NOT use `onUnhandledRequest: 'error'` in browser/development mode. It will throw on every real API call the app makes. Use `'error'` only in strict test environments, and `'bypass'` in browsers.\n- Do NOT commit `public/mockServiceWorker.js` if you're using a monorepo or multiple static servers — each origin needs its own copy. Regenerate with `msw init <public-dir>` per project.\n- Do NOT write handlers that `return undefined` accidentally — always return a `Response` or `HttpResponse`. Returning nothing from a handler causes MSW to fall through with no response, resulting in a network error.",
  "version": "1.0.0",
  "updated_at": "2026-05-28T00:00:00Z",
  "category": "testing"
}
