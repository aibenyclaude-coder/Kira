{
  "id": "community.setup-husky-lint-staged.v1",
  "keywords": [
    "husky",
    "lint-staged",
    "pre-commit",
    "git hooks",
    "add husky",
    "setup git hooks",
    "pre-commit hooks",
    "commit hooks",
    "pre-commit lint"
  ],
  "contexts": [
    "nextjs",
    "react",
    "typescript",
    "javascript",
    "nodejs"
  ],
  "title": "Set Up Husky + lint-staged for Pre-Commit Hooks",
  "summary": "Install Husky and lint-staged to automatically lint, format, and type-check staged files before every git commit — keeping the codebase clean without slowing down the full CI run.",
  "source": "community",
  "declaration": "I will set up Husky and lint-staged to enforce code quality on every commit. Staged files will be linted and formatted automatically before the commit is allowed through. If you want to skip type-checking in the pre-commit hook (because it is too slow), say so before I begin.",
  "instructions": "## Set Up Husky + lint-staged for Pre-Commit Hooks\n\n### 1. Pre-flight checks\n- Verify this is a git repository: `git rev-parse --git-dir`. If it is not, run `git init` first — Husky requires a git repo to install hooks.\n- Verify `package.json` exists at the project root.\n- Check whether ESLint is already configured (look for `.eslintrc.*`, `eslint.config.*`, or `eslint` in `package.json` devDependencies). Lint-staged calls these tools — they must already be installed.\n- Check whether Prettier is configured (look for `.prettierrc`, `prettier.config.*`, or `prettier` in devDependencies). If present, lint-staged will auto-format.\n- If neither ESLint nor Prettier is installed, set them up first — lint-staged has nothing useful to run without them.\n\n### 2. Install Husky and lint-staged\n- Install as devDependencies:\n```bash\nnpm install --save-dev husky lint-staged\n```\n- Or with pnpm: `pnpm add -D husky lint-staged`\n- Or with yarn: `yarn add -D husky lint-staged`\n- Verify both appear in `devDependencies` in `package.json`.\n\n### 3. Initialize Husky\n- Run the Husky init command:\n```bash\nnpx husky init\n```\n- This does two things:\n  1. Adds `\"prepare\": \"husky\"` to `package.json` scripts (runs `husky` after `npm install`, which installs hooks into `.git/hooks/`).\n  2. Creates a `.husky/` directory with a sample `pre-commit` file.\n- Verify `.husky/` directory was created and `.husky/pre-commit` exists.\n- Commit `.husky/` to version control — hooks must be checked in so teammates get them automatically on `npm install`.\n\n### 4. Configure lint-staged in package.json\n- Add a `lint-staged` configuration block to `package.json`:\n\n**For projects with ESLint + Prettier:**\n```json\n\"lint-staged\": {\n  \"*.{js,jsx,ts,tsx}\": [\n    \"eslint --fix --max-warnings=0\",\n    \"prettier --write\"\n  ],\n  \"*.{json,md,css,scss}\": [\n    \"prettier --write\"\n  ]\n}\n```\n\n**For projects with ESLint only (no Prettier):**\n```json\n\"lint-staged\": {\n  \"*.{js,jsx,ts,tsx}\": [\n    \"eslint --fix --max-warnings=0\"\n  ]\n}\n```\n\n**For projects using Biome instead of ESLint/Prettier:**\n```json\n\"lint-staged\": {\n  \"*.{js,jsx,ts,tsx,json}\": [\n    \"biome check --apply\"\n  ]\n}\n```\n\n- `--fix` auto-fixes fixable ESLint errors and re-stages the fixed files.\n- `--max-warnings=0` makes lint-staged fail if any warnings exist (not just errors). Remove this if your codebase has legitimate suppressable warnings.\n- Prettier `--write` overwrites the file in place and re-stages it automatically (lint-staged handles the re-staging).\n\n### 5. Update the pre-commit hook\n- Edit `.husky/pre-commit` to run lint-staged:\n```sh\nnpx lint-staged\n```\n- Replace any existing content in `.husky/pre-commit` with just this command.\n- Make sure the file is executable (Husky handles this, but if on Linux/macOS you can verify with `ls -la .husky/`).\n\n### 6. Optional: Add TypeScript type-checking\n- Type-checking the entire project (not just staged files) during pre-commit is optional — it can add 30–90 seconds to every commit.\n- If the team wants it, add a separate check that runs `tsc --noEmit`:\n\nCreate `.husky/pre-commit` with:\n```sh\nnpx lint-staged\nnpx tsc --noEmit\n```\n\n- IMPORTANT: `tsc --noEmit` checks the ENTIRE project, not just staged files. It catches cross-file type errors that staged-file linting cannot. However, it is slow on large codebases. For large projects, run type-checking only in CI and skip it in the pre-commit hook.\n- If the project uses Next.js, `tsc` reads `tsconfig.json` automatically. No extra configuration needed.\n\n### 7. Optional: Add a commit-msg hook for conventional commits\n- If the project uses conventional commits (feat:, fix:, docs:, etc.), add commitlint:\n```bash\nnpm install --save-dev @commitlint/cli @commitlint/config-conventional\necho \"export default { extends: ['@commitlint/config-conventional'] };\" > commitlint.config.mjs\necho \"npx --no -- commitlint --edit $1\" > .husky/commit-msg\n```\n- This enforces commit message format (e.g., `feat: add login page`) on every commit.\n- This is optional — only add it if the team has agreed to use conventional commits.\n\n### 8. Test the setup\n- Make a small intentional ESLint violation in a staged file:\n  - Add `var x = 1;` to any `.ts` file (ESLint `no-var` rule will catch this).\n- Stage the file: `git add <file>`\n- Try to commit: `git commit -m 'test: husky'`\n- The pre-commit hook should run lint-staged, catch the violation, and abort the commit.\n- Fix the violation, re-stage, and commit — this time it should succeed.\n- Verify the commit was created: `git log --oneline -1`\n\n### 9. Ensure teammates get hooks automatically\n- Because `\"prepare\": \"husky\"` is in `package.json`, running `npm install` (or `pnpm install`) in a fresh clone automatically installs the hooks.\n- Add a note to the project README or CONTRIBUTING.md: \"Run `npm install` to set up git hooks.\"\n- For CI environments, Husky auto-detects CI (via `CI` environment variable) and skips hook installation. No manual exclusion needed.\n\n### Common Errors & Fixes\n- **Hook not running on commit** — Husky was not initialized. Run `npx husky init` and verify `.git/hooks/pre-commit` exists (it should be a symlink or script pointing to `.husky/pre-commit`).\n- **`husky: command not found`** — `npm install` was not run after adding Husky, so the `prepare` script did not run. Run `npm install` again.\n- **`lint-staged: command not found`** — Same cause. Run `npm install`.\n- **Hook runs but lint-staged finds no files** — The glob patterns in `lint-staged` config do not match the staged files. Check the patterns (e.g., `*.{ts,tsx}` vs `**/*.ts`). Lint-staged uses micromatch — test patterns with `npx micromatch '**/*.tsx' 'src/components/Button.tsx'`.\n- **ESLint fails on auto-generated files** — Add those files to `.eslintignore` (or `eslint.config.*` ignores). Lint-staged will then skip them.\n- **Pre-commit takes too long** — If `tsc --noEmit` is in the hook, remove it and run type-checking only in CI. Or use `tsc --incremental` if the tsconfig supports it.\n- **Hook not working after cloning on a new machine** — The teammate ran `npm ci` (which skips `prepare`). Instruct them to run `npm install` first, or change to `npm ci && npm run prepare` in their setup steps.\n- **On Windows: `Permission denied` running hook** — Git for Windows sometimes loses execute permissions. Run: `git update-index --chmod=+x .husky/pre-commit`.\n- **ESLint `--fix` modified files but commit was rejected** — Lint-staged re-stages fixed files, but the commit succeeds only if there are NO remaining errors. The `--fix` flag fixes what it can; the commit fails if unfixable errors remain (which is correct behavior).\n\n### What NOT to Do\n- Do NOT use the deprecated `\"husky\": { \"hooks\": {...} }` format in `package.json` — that was Husky v4 syntax. Husky v8+ uses `.husky/` directory files exclusively.\n- Do NOT run `tsc --noEmit` on every commit in large codebases — it will slow everyone down and developers will bypass the hooks with `git commit --no-verify`.\n- Do NOT add `git add` inside lint-staged commands — lint-staged handles re-staging automatically after `--write` or `--fix` operations.\n- Do NOT lint `node_modules` — ensure your ESLint config ignores `node_modules`. Lint-staged only passes staged files, so this is not usually a problem, but be careful with glob patterns that are overly broad.\n- Do NOT commit `.git/hooks/` — only commit `.husky/`. The `.git/` directory is not tracked by git. Husky's `prepare` script installs hooks into `.git/hooks/` from `.husky/` on each `npm install`.\n- Do NOT skip the `prepare` script — it is what makes hooks install automatically for new developers. Without it, only the person who set up Husky has working hooks.\n- Do NOT allow `--no-verify` as a routine workaround — if developers routinely bypass hooks, the hooks are either too slow or too strict. Fix the root cause instead.",
  "version": "1.0.0",
  "updated_at": "2026-06-25T00:00:00Z",
  "category": "tooling"
}
