{
  "id": "community.setup-convex-nextjs.v1",
  "keywords": [
    "convex",
    "setup convex",
    "add convex",
    "convex nextjs",
    "convex database",
    "convex backend",
    "real-time database",
    "realtime backend",
    "convex functions",
    "reactive database"
  ],
  "contexts": [
    "nextjs",
    "react",
    "typescript",
    "app-router"
  ],
  "title": "Set up Convex real-time backend in a Next.js App Router project",
  "summary": "Full Convex integration for Next.js — install the SDK, bootstrap the project, write typed server functions (queries, mutations, actions), configure the React provider, and connect components with reactive hooks.",
  "source": "community",
  "declaration": "I will set up Convex as the real-time backend for this Next.js project. Convex replaces a traditional database + ORM + API layer with a single TypeScript-first platform. It provides reactive queries (components auto-update when data changes), server-side mutations and actions, file storage, and scheduled jobs. If you only need a traditional REST API or already have Prisma/Drizzle configured, tell me before I begin — Convex works differently.",
  "instructions": "## Set up Convex in Next.js (App Router)\n\n### 0. Prerequisites\n- Node.js >= 18.\n- A Convex account at https://dashboard.convex.dev (sign in with GitHub or Google — free tier is generous).\n- The project uses Next.js App Router (`app/` directory).\n\n### 1. Install dependencies\n```bash\nnpm install convex\n```\n- `convex` is the only package needed for both server functions and client-side hooks.\n- Do NOT install `@convex-dev/auth` unless you want Convex-native auth — standard Clerk/Auth.js integrations use a different approach.\n\n### 2. Initialize Convex\n```bash\nnpx convex dev\n```\n- This command authenticates your CLI, prompts you to create or select a Convex project, and generates:\n  - `convex.json` — project configuration (commit this).\n  - `convex/_generated/` — auto-generated API types (do NOT edit; commit the directory but add to `.gitignore` if you prefer).\n- Leave this terminal running during development — it watches the `convex/` directory and hot-reloads functions.\n- The command also prints your deployment URL: `https://<slug>.convex.cloud`. This becomes `NEXT_PUBLIC_CONVEX_URL`.\n\n### 3. Environment variables\n- Create or update `.env.local`:\n```\nNEXT_PUBLIC_CONVEX_URL=https://<your-slug>.convex.cloud\n```\n- The `NEXT_PUBLIC_` prefix is required — the client SDK needs this in the browser.\n- Do NOT add a secret key to `.env.local`. Convex security is managed through function-level auth checks, not API keys.\n- Verify `.env.local` is in `.gitignore`.\n\n### 4. Wrap the app with ConvexClientProvider\n- Create `app/providers.tsx`:\n```tsx\n'use client'\n\nimport { ConvexProvider, ConvexReactClient } from 'convex/react'\n\nconst convex = new ConvexReactClient(process.env.NEXT_PUBLIC_CONVEX_URL!)\n\nexport function Providers({ children }: { children: React.ReactNode }) {\n  return <ConvexProvider client={convex}>{children}</ConvexProvider>\n}\n```\n- Update `app/layout.tsx` to wrap children:\n```tsx\nimport { Providers } from './providers'\n\nexport default function RootLayout({ children }: { children: React.ReactNode }) {\n  return (\n    <html lang=\"en\">\n      <body>\n        <Providers>{children}</Providers>\n      </body>\n    </html>\n  )\n}\n```\n- The `ConvexReactClient` instance is created once outside the component — do NOT create it inside a component render or useEffect.\n\n### 5. Define your schema (`convex/schema.ts`)\n- Create the `convex/` directory in your project root:\n```typescript\nimport { defineSchema, defineTable } from 'convex/server'\nimport { v } from 'convex/values'\n\nexport default defineSchema({\n  tasks: defineTable({\n    text: v.string(),\n    isCompleted: v.boolean(),\n    userId: v.optional(v.string()),\n    createdAt: v.number(),\n  }),\n})\n```\n- The schema is optional but strongly recommended — it enables TypeScript inference and runtime validation.\n- Convex validators (`v.string()`, `v.number()`, `v.boolean()`, `v.array()`, `v.object()`) mirror TypeScript types.\n- After saving, `npx convex dev` automatically pushes the schema to the cloud.\n\n### 6. Write server functions\n\n#### 6a. Query (read data — reactive)\nCreate `convex/tasks.ts`:\n```typescript\nimport { query, mutation } from './_generated/server'\nimport { v } from 'convex/values'\n\nexport const list = query({\n  args: {},\n  handler: async (ctx) => {\n    return await ctx.db.query('tasks').order('desc').collect()\n  },\n})\n\nexport const listByUser = query({\n  args: { userId: v.string() },\n  handler: async (ctx, args) => {\n    return await ctx.db\n      .query('tasks')\n      .filter((q) => q.eq(q.field('userId'), args.userId))\n      .collect()\n  },\n})\n```\n- Queries are read-only and run reactively — any client subscribed to a query re-renders automatically when the underlying data changes.\n- Do NOT perform side effects (HTTP calls, writes) inside a query. Use mutations or actions for that.\n\n#### 6b. Mutation (write data)\n```typescript\nexport const create = mutation({\n  args: {\n    text: v.string(),\n    userId: v.optional(v.string()),\n  },\n  handler: async (ctx, args) => {\n    const taskId = await ctx.db.insert('tasks', {\n      text: args.text,\n      isCompleted: false,\n      userId: args.userId,\n      createdAt: Date.now(),\n    })\n    return taskId\n  },\n})\n\nexport const toggleComplete = mutation({\n  args: { id: v.id('tasks') },\n  handler: async (ctx, args) => {\n    const task = await ctx.db.get(args.id)\n    if (!task) throw new Error('Task not found')\n    await ctx.db.patch(args.id, { isCompleted: !task.isCompleted })\n  },\n})\n\nexport const remove = mutation({\n  args: { id: v.id('tasks') },\n  handler: async (ctx, args) => {\n    await ctx.db.delete(args.id)\n  },\n})\n```\n- `v.id('tableName')` is a typed document ID — Convex validates it at runtime.\n- `ctx.db.patch` does a partial update; `ctx.db.replace` replaces the entire document.\n\n#### 6c. Action (external API calls, long-running work)\n```typescript\nimport { action } from './_generated/server'\nimport { v } from 'convex/values'\n\nexport const generateSummary = action({\n  args: { taskId: v.id('tasks') },\n  handler: async (ctx, args) => {\n    // Actions can call external APIs\n    const task = await ctx.runQuery(api.tasks.getById, { id: args.taskId })\n    const response = await fetch('https://api.openai.com/v1/...', { ... })\n    const summary = await response.json()\n    await ctx.runMutation(api.tasks.updateSummary, { id: args.taskId, summary })\n    return summary\n  },\n})\n```\n- Actions can call external HTTP APIs and run database operations via `ctx.runQuery` / `ctx.runMutation`.\n- Actions do NOT run reactively — they are called imperatively (like an RPC).\n\n### 7. Use hooks in React components\n```tsx\n'use client'\n\nimport { useQuery, useMutation } from 'convex/react'\nimport { api } from '@/convex/_generated/api'\n\nexport function TaskList() {\n  const tasks = useQuery(api.tasks.list)\n  const createTask = useMutation(api.tasks.create)\n  const toggleTask = useMutation(api.tasks.toggleComplete)\n\n  if (tasks === undefined) return <div>Loading...</div>\n\n  return (\n    <div>\n      <button onClick={() => createTask({ text: 'New task' })}>\n        Add Task\n      </button>\n      {tasks.map((task) => (\n        <div key={task._id} onClick={() => toggleTask({ id: task._id })}>\n          {task.isCompleted ? '✓' : '○'} {task.text}\n        </div>\n      ))}\n    </div>\n  )\n}\n```\n- `useQuery` returns `undefined` while loading, then the data — always check for `undefined`.\n- `useMutation` returns a function that sends the mutation to Convex. It handles optimistic updates automatically.\n- `api` is the auto-generated typed API object from `convex/_generated/api.ts`. Import from there — never hardcode function names as strings.\n\n### 8. Authentication integration\n- Convex integrates with Clerk, Auth.js, and other providers.\n\n#### With Clerk:\n```bash\nnpm install @clerk/nextjs\n```\nUpdate `app/providers.tsx`:\n```tsx\n'use client'\n\nimport { ClerkProvider, useAuth } from '@clerk/nextjs'\nimport { ConvexProviderWithClerk } from 'convex/react-clerk'\nimport { ConvexReactClient } from 'convex/react'\n\nconst convex = new ConvexReactClient(process.env.NEXT_PUBLIC_CONVEX_URL!)\n\nexport function Providers({ children }: { children: React.ReactNode }) {\n  return (\n    <ClerkProvider publishableKey={process.env.NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY!}>\n      <ConvexProviderWithClerk client={convex} useAuth={useAuth}>\n        {children}\n      </ConvexProviderWithClerk>\n    </ClerkProvider>\n  )\n}\n```\nIn server functions, access the user identity:\n```typescript\nexport const listMyTasks = query({\n  handler: async (ctx) => {\n    const identity = await ctx.auth.getUserIdentity()\n    if (!identity) throw new Error('Unauthenticated')\n    return ctx.db\n      .query('tasks')\n      .filter((q) => q.eq(q.field('userId'), identity.subject))\n      .collect()\n  },\n})\n```\n\n### 9. Deployment (production)\n```bash\nnpx convex deploy\n```\n- This pushes your functions to the production Convex deployment.\n- Set `NEXT_PUBLIC_CONVEX_URL` in your Vercel/hosting platform to the production URL (found in Convex dashboard).\n- You do NOT need to run `convex dev` in production — it's development-only.\n\n### Common errors & fixes\n- **`ConvexProvider not found` / hooks throw outside provider** → The component using `useQuery`/`useMutation` is not wrapped in `ConvexProvider`. Check that `Providers` wraps the root layout and that `'use client'` is set on the providers file.\n- **`useQuery` always returns `undefined`** → The query function doesn't exist or has a typo. Check that the function is exported from its `convex/*.ts` file and that `npx convex dev` is running.\n- **`Argument validation error`** → You passed wrong types to a mutation/query. The validator in the `args` block is enforced at runtime. Check the function signature.\n- **Changes not appearing** → `npx convex dev` is not running. It must be active in development to push function changes.\n- **`Cannot use import/require in convex functions`** → Convex functions run in a sandboxed environment. You cannot use native Node.js modules (`fs`, `crypto`, etc.) in queries/mutations. Use actions for external calls.\n- **Data not reactive / component not re-rendering** → You are calling the function directly instead of using `useQuery`. Only `useQuery` creates a reactive subscription.\n- **TypeScript error on `api.tasks.myFunction`** → The `_generated/api.ts` file is stale. Make sure `npx convex dev` is running, or run `npx convex codegen` manually.\n\n### What NOT to do\n- Do NOT call Convex functions directly from Server Components with `fetch`. Use the Convex HTTP client (`new ConvexHttpClient(url)`) from a Server Component, or use `useQuery` in Client Components.\n- Do NOT perform side effects (HTTP calls, sending emails) inside queries or mutations — only in actions. Side effects in queries/mutations will cause errors and won't be retried correctly.\n- Do NOT store secrets in Convex function code — they will be visible in the Convex dashboard. Use Convex environment variables (`npx convex env set SECRET value`) for secrets accessed in actions.\n- Do NOT create new `ConvexReactClient` instances inside React components. Create one instance at module level and pass it to the provider.\n- Do NOT manually edit files in `convex/_generated/` — they are overwritten on every `convex dev` / `convex deploy`.\n- Do NOT skip schema definition for production apps. Without a schema, there is no runtime validation and TypeScript types won't work.\n- Do NOT use Convex as a replacement for file storage — store large files in Convex File Storage or S3/R2 and only store the reference URL in Convex.\n- Do NOT check `identity.tokenIdentifier` for user ID in queries — use `identity.subject` which is the stable user ID.",
  "version": "1.0.0",
  "updated_at": "2026-06-30T00:00:00Z",
  "category": "database"
}
