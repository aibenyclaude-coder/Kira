{
  "id": "community.setup-neon.v1",
  "keywords": [
    "neon",
    "setup neon",
    "neon database",
    "neon postgres",
    "serverless postgres",
    "neon db",
    "neon serverless",
    "add neon",
    "connect neon"
  ],
  "contexts": [
    "nextjs",
    "typescript",
    "nodejs"
  ],
  "title": "Connect to Neon Serverless Postgres",
  "summary": "Create a Neon project, connect via @neondatabase/serverless, integrate with Drizzle or Prisma ORM, and configure connection pooling for Next.js serverless/edge environments.",
  "source": "community",
  "declaration": "I will connect this project to Neon serverless Postgres. This installs the Neon driver, sets up the connection, and integrates with your ORM (Drizzle or Prisma). Neon works in both Node.js and Edge runtimes. If you prefer a different database provider, say so before I begin.",
  "instructions": "## Connect to Neon Serverless Postgres\n\n### 1. Create a Neon project\n- Go to [neon.tech](https://neon.tech) and sign up (free tier available).\n- Click **New Project**, choose a region close to your deployment target (e.g., US East for Vercel).\n- Copy the **Connection String** from the dashboard. It looks like:\n  `postgresql://user:password@ep-xyz.us-east-2.aws.neon.tech/neondb?sslmode=require`\n- Neon creates one database (`neondb`) and one branch (`main`) by default. Use branches for dev/staging environments.\n\n### 2. Set environment variable\n- Add to `.env` (and `.env.local` for local development):\n  ```\n  DATABASE_URL=postgresql://user:password@ep-xyz.us-east-2.aws.neon.tech/neondb?sslmode=require\n  ```\n- Add `DATABASE_URL` to your Vercel/deployment environment variables too.\n- Do NOT commit `.env` to git. Ensure it is in `.gitignore`.\n- If you need connection pooling (recommended for serverless): Neon provides a **pooled connection string** (uses PgBouncer). Copy the pooled URL from the dashboard and use it as `DATABASE_URL`. Keep the direct URL as `DIRECT_URL` for migrations.\n\n### 3. Install the Neon serverless driver\n- Run: `npm install @neondatabase/serverless`.\n- The Neon serverless driver uses HTTP or WebSockets instead of TCP — this allows it to work in Edge runtimes (Vercel Edge Functions, Cloudflare Workers) where raw TCP is not available.\n\n### 4a. Use with Drizzle ORM (recommended)\n- Install Drizzle: `npm install drizzle-orm` and `npm install -D drizzle-kit`.\n- Create `src/db/index.ts`:\n  ```ts\n  import { drizzle } from 'drizzle-orm/neon-http';\n  import { neon } from '@neondatabase/serverless';\n\n  const sql = neon(process.env.DATABASE_URL!);\n  export const db = drizzle(sql);\n  ```\n- For **WebSocket mode** (long-lived connections, better for serverful Node.js):\n  ```ts\n  import { drizzle } from 'drizzle-orm/neon-serverless';\n  import { Pool } from '@neondatabase/serverless';\n\n  const pool = new Pool({ connectionString: process.env.DATABASE_URL });\n  export const db = drizzle(pool);\n  ```\n- Use `neon-http` for Edge / serverless; use `neon-serverless` (WebSocket Pool) for Node.js servers.\n- Define a schema in `src/db/schema.ts`:\n  ```ts\n  import { pgTable, serial, text, timestamp } from 'drizzle-orm/pg-core';\n\n  export const users = pgTable('users', {\n    id: serial('id').primaryKey(),\n    email: text('email').notNull().unique(),\n    name: text('name'),\n    createdAt: timestamp('created_at').defaultNow(),\n  });\n  ```\n- Add `drizzle.config.ts` at the project root:\n  ```ts\n  import type { Config } from 'drizzle-kit';\n  export default {\n    schema: './src/db/schema.ts',\n    out: './drizzle',\n    dialect: 'postgresql',\n    dbCredentials: {\n      url: process.env.DATABASE_URL!,\n    },\n  } satisfies Config;\n  ```\n- Run migrations: `npx drizzle-kit generate` then `npx drizzle-kit migrate`.\n\n### 4b. Use with Prisma ORM (alternative)\n- Install Prisma: `npm install prisma @prisma/client` and run `npx prisma init`.\n- Update `prisma/schema.prisma`:\n  ```prisma\n  datasource db {\n    provider  = \"postgresql\"\n    url       = env(\"DATABASE_URL\")\n    directUrl = env(\"DIRECT_URL\")\n  }\n  ```\n- Set both env vars: `DATABASE_URL` (pooled) and `DIRECT_URL` (direct, for migrations).\n- Install Prisma's Neon adapter for Edge support: `npm install @prisma/adapter-neon`.\n- Update `src/lib/prisma.ts`:\n  ```ts\n  import { PrismaClient } from '@prisma/client';\n  import { PrismaNeon } from '@prisma/adapter-neon';\n  import { Pool } from '@neondatabase/serverless';\n\n  const pool = new Pool({ connectionString: process.env.DATABASE_URL });\n  const adapter = new PrismaNeon(pool);\n\n  const globalForPrisma = global as unknown as { prisma: PrismaClient };\n  export const prisma =\n    globalForPrisma.prisma ||\n    new PrismaClient({ adapter });\n\n  if (process.env.NODE_ENV !== 'production') globalForPrisma.prisma = prisma;\n  ```\n- Run migrations: `npx prisma migrate dev`.\n\n### 5. Use the database in Next.js\n- In a Server Component or API route:\n  ```ts\n  // app/api/users/route.ts\n  import { db } from '@/db';\n  import { users } from '@/db/schema';\n\n  export async function GET() {\n    const allUsers = await db.select().from(users);\n    return Response.json(allUsers);\n  }\n  ```\n- For Edge runtime, add `export const runtime = 'edge';` to the route file and use the HTTP Drizzle driver.\n\n### 6. Branching for development (optional but recommended)\n- Neon branches are instant, copy-on-write snapshots of the database — use them like git branches.\n- Create a dev branch from the Neon dashboard or CLI: `neon branch create --name dev`.\n- Use the dev branch connection string in `.env.local` so local dev has isolated data.\n- Merge the dev branch back to main when ready, or reset it for clean test data.\n\n### 7. Verify the connection\n- Add a health check route `app/api/health/route.ts`:\n  ```ts\n  import { neon } from '@neondatabase/serverless';\n  const sql = neon(process.env.DATABASE_URL!);\n  export async function GET() {\n    const result = await sql`SELECT NOW()`;\n    return Response.json({ time: result[0].now });\n  }\n  ```\n- Visit `/api/health` — you should see the current timestamp from Neon.\n\n### Common errors & fixes\n- **`Error: SSL SYSCALL error: EOF detected`** — Missing `?sslmode=require` at the end of the connection string. Always include it for Neon.\n- **`too many connections`** — You are using a direct connection without pooling in a serverless context. Switch to the **pooled** connection string from the Neon dashboard.\n- **`fetch failed` on Edge runtime** — You are using the WebSocket Pool driver (`neon-serverless`) in an Edge function. Use `neon-http` driver for Edge runtimes.\n- **Prisma migration fails on pooled URL** — Migrations must run against the direct (non-pooled) URL. Set `directUrl = env(\"DIRECT_URL\")` in `schema.prisma`.\n- **`Cannot find module 'drizzle-orm/neon-http'`** — Drizzle ORM is not installed. Run `npm install drizzle-orm`.\n- **Local dev not connecting** — Check `.env.local` has `DATABASE_URL`. Next.js loads `.env.local` automatically; if using a plain Node script, use `dotenv`.\n- **Slow first query (cold start)** — Neon serverless branches auto-suspend after inactivity. The first query after suspension takes ~500ms. This is normal; use Neon's **always-on** option or a connection keep-alive for latency-sensitive apps.\n\n### What NOT to do\n- Do NOT use the direct connection string in production serverless without pooling — you will exhaust Postgres connection limits under load. Always use the pooled URL.\n- Do NOT commit `DATABASE_URL` to git or `.env` files that are checked in — use environment variables in your deployment platform.\n- Do NOT run `npx prisma migrate dev` against the pooled URL — it will fail. Always point migrations at the direct URL via `directUrl`.\n- Do NOT use the WebSocket Pool driver (`neon-serverless`) in Cloudflare Workers or Vercel Edge Functions — raw WebSocket upgrade is not supported in all edge runtimes. Use `neon-http`.\n- Do NOT create a new `neon()` or `Pool()` instance on every request — instantiate once at module level and reuse.\n- Do NOT skip `?sslmode=require` in the connection string — Neon requires TLS for all connections.\n- Do NOT use Neon's free tier for high-throughput production workloads — it auto-suspends after 5 minutes of inactivity. Upgrade to a paid plan or use a dedicated compute for production.",
  "version": "1.0.0",
  "updated_at": "2026-04-22T00:00:00Z",
  "category": "backend"
}
