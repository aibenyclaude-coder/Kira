{
  "id": "community.setup-upstash-redis.v1",
  "keywords": [
    "upstash",
    "upstash redis",
    "add redis",
    "serverless redis",
    "rate limiting",
    "rate limit",
    "redis edge",
    "@upstash/redis",
    "@upstash/ratelimit",
    "kv store",
    "redis nextjs"
  ],
  "contexts": [
    "nextjs",
    "typescript",
    "nodejs",
    "vercel"
  ],
  "title": "Set up Upstash Redis in a Next.js project",
  "summary": "Install @upstash/redis, configure REST credentials, add caching and rate limiting with @upstash/ratelimit — works on Vercel Edge, Cloudflare Workers, and Node.js.",
  "source": "community",
  "declaration": "I will set up Upstash Redis using the HTTP-based @upstash/redis client. This is the correct Redis client for Next.js Edge Runtime and Vercel deployments. If you need traditional TCP Redis (ioredis / node-redis), say so before I begin.",
  "instructions": "## Set up Upstash Redis in a Next.js project\n\n### 1. Create an Upstash database\n- Go to console.upstash.com and create a new Redis database.\n- Select the region closest to your deployment (e.g. `us-east-1` for Vercel's default).\n- Choose **Regional** type for most use cases. Choose **Global** only if you need multi-region replication.\n- After creation, go to the database's **REST API** tab and copy the `UPSTASH_REDIS_REST_URL` and `UPSTASH_REDIS_REST_TOKEN`.\n\n### 2. Add environment variables\n- Add to `.env.local` (NEVER commit this file):\n  ```\n  UPSTASH_REDIS_REST_URL=https://your-db.upstash.io\n  UPSTASH_REDIS_REST_TOKEN=your-token-here\n  ```\n- NEVER prefix these with `NEXT_PUBLIC_` — they are server-side secrets.\n- For Vercel deployment: add these in the Vercel dashboard under Project → Settings → Environment Variables.\n\n### 3. Install packages\n- For Redis operations only:\n  ```\n  npm install @upstash/redis\n  ```\n- For rate limiting (optional but common):\n  ```\n  npm install @upstash/ratelimit\n  ```\n- Do NOT install `ioredis` or `redis` (node-redis) — those use TCP and break on Edge Runtime.\n\n### 4. Create the Redis client (`lib/redis.ts`)\n- Create a shared client instance:\n  ```ts\n  import { Redis } from '@upstash/redis';\n\n  export const redis = new Redis({\n    url: process.env.UPSTASH_REDIS_REST_URL!,\n    token: process.env.UPSTASH_REDIS_REST_TOKEN!,\n  });\n  ```\n- Alternatively, use the auto-env constructor (reads the env vars automatically):\n  ```ts\n  import { Redis } from '@upstash/redis';\n\n  export const redis = Redis.fromEnv();\n  ```\n- `Redis.fromEnv()` reads `UPSTASH_REDIS_REST_URL` and `UPSTASH_REDIS_REST_TOKEN` automatically.\n- IMPORTANT: Create this client once as a module-level singleton. Do NOT instantiate inside request handlers.\n\n### 5. Basic Redis operations\n- Use the client in any Server Component, API Route, or Server Action:\n  ```ts\n  import { redis } from '@/lib/redis';\n\n  // Set a value (with optional TTL in seconds)\n  await redis.set('key', 'value', { ex: 60 });\n\n  // Get a value (auto-parses JSON if stored as object)\n  const value = await redis.get<string>('key');\n\n  // Increment a counter\n  await redis.incr('page-views');\n\n  // Delete a key\n  await redis.del('key');\n\n  // Check expiry\n  await redis.ttl('key');\n  ```\n- All methods are async and return typed values. Pass a generic (`get<string>`, `get<User>`) to get typed results.\n- Objects are automatically serialized/deserialized as JSON.\n\n### 6. Caching pattern (API routes or Server Components)\n- Use a stale-while-revalidate cache helper:\n  ```ts\n  import { redis } from '@/lib/redis';\n\n  export async function getCached<T>(\n    key: string,\n    fetcher: () => Promise<T>,\n    ttlSeconds = 60\n  ): Promise<T> {\n    const cached = await redis.get<T>(key);\n    if (cached !== null) return cached;\n    const fresh = await fetcher();\n    await redis.set(key, fresh, { ex: ttlSeconds });\n    return fresh;\n  }\n  ```\n- Call it:\n  ```ts\n  const posts = await getCached('blog:posts', fetchPostsFromDB, 300);\n  ```\n\n### 7. Rate limiting with @upstash/ratelimit\n- Create the rate limiter (`lib/ratelimit.ts`):\n  ```ts\n  import { Ratelimit } from '@upstash/ratelimit';\n  import { redis } from './redis';\n\n  // Allow 10 requests per 10 seconds per identifier\n  export const ratelimit = new Ratelimit({\n    redis,\n    limiter: Ratelimit.slidingWindow(10, '10 s'),\n    analytics: true,\n    prefix: 'rl',\n  });\n  ```\n- Use in an API route or middleware:\n  ```ts\n  import { ratelimit } from '@/lib/ratelimit';\n  import { NextRequest, NextResponse } from 'next/server';\n\n  export async function POST(req: NextRequest) {\n    const ip = req.ip ?? req.headers.get('x-forwarded-for') ?? 'unknown';\n    const { success, remaining, reset } = await ratelimit.limit(ip);\n\n    if (!success) {\n      return NextResponse.json(\n        { error: 'Rate limit exceeded' },\n        {\n          status: 429,\n          headers: {\n            'Retry-After': String(Math.ceil((reset - Date.now()) / 1000)),\n            'X-RateLimit-Remaining': String(remaining),\n          },\n        }\n      );\n    }\n    // ... handle request\n  }\n  ```\n- Rate limiter algorithms: `slidingWindow` (smooth, accurate), `fixedWindow` (simpler, cheaper), `tokenBucket` (allows bursts).\n\n### 8. Use in Next.js Middleware (Edge)\n- Upstash Redis works in `middleware.ts` without any extra config:\n  ```ts\n  import { NextResponse } from 'next/server';\n  import type { NextRequest } from 'next/server';\n  import { ratelimit } from './lib/ratelimit';\n\n  export async function middleware(req: NextRequest) {\n    const ip = req.ip ?? 'unknown';\n    const { success } = await ratelimit.limit(ip);\n    if (!success) {\n      return new NextResponse('Too Many Requests', { status: 429 });\n    }\n    return NextResponse.next();\n  }\n\n  export const config = {\n    matcher: '/api/:path*',\n  };\n  ```\n- This works because `@upstash/redis` uses `fetch` (HTTP) not TCP — fully compatible with Edge Runtime.\n\n### 9. Verify the setup\n- Add a test route `app/api/redis-test/route.ts`:\n  ```ts\n  import { redis } from '@/lib/redis';\n  import { NextResponse } from 'next/server';\n\n  export async function GET() {\n    await redis.set('test', 'hello', { ex: 10 });\n    const val = await redis.get('test');\n    return NextResponse.json({ value: val });\n  }\n  ```\n- Run `npm run dev` and visit `/api/redis-test`. Expect `{ \"value\": \"hello\" }`.\n- Delete the test route when done.\n\n### Common errors & fixes\n- **`Edge Runtime: fetch not defined` or TCP error** — You installed `ioredis` or `redis` (node-redis) instead of `@upstash/redis`. Uninstall and reinstall the correct package.\n- **`AuthenticationError: WRONGPASS`** — Token is wrong or expired. Re-copy the REST token from the Upstash console REST API tab.\n- **`TypeError: Redis is not a constructor`** — You are importing from `ioredis`. Change to `import { Redis } from '@upstash/redis'`.\n- **`null` returned from `get()`** — Key does not exist or has expired. Check TTL with `await redis.ttl('key')`. Distinguish `null` (missing) from `0` (exists, no TTL).\n- **`UPSTASH_REDIS_REST_URL is not defined`** — Env var not loaded. Verify `.env.local` (local) and Vercel project settings (production). Restart dev server after changing `.env.local`.\n- **Rate limiter not working** — Ensure the `prefix` value is consistent across deployments. If you change the prefix, existing counters are orphaned.\n- **`Maximum update depth exceeded`** — You are calling `redis` inside a React render. Use it in Server Components (async functions), API routes, or Server Actions only.\n\n### What NOT to do\n- Do NOT use `ioredis` or `node-redis` with Next.js Edge Runtime or Vercel Functions — they use TCP and will throw at runtime.\n- Do NOT expose `UPSTASH_REDIS_REST_TOKEN` to the client via `NEXT_PUBLIC_` prefix — anyone can read your Redis database.\n- Do NOT create a new Redis instance inside every request handler — instantiate once at module level.\n- Do NOT store sensitive PII (passwords, full credit card numbers) in Redis without encryption — it is a cache, not a secure vault.\n- Do NOT rely on Redis as the only storage for critical data — persist important data in your primary database.\n- Do NOT use `set` without a TTL for cache entries — memory grows unbounded and you will hit the Upstash free-tier limit.\n- Do NOT share the same Upstash database between production and dev/staging — create separate databases per environment.",
  "version": "1.0.0",
  "updated_at": "2026-06-29T00:00:00Z"
}
