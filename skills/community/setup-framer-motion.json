{
  "id": "community.setup-framer-motion.v1",
  "keywords": [
    "framer motion",
    "framer",
    "animation",
    "animations",
    "add animation",
    "add framer motion",
    "setup framer motion",
    "react animation",
    "page transitions",
    "motion",
    "animate",
    "gesture animation",
    "spring animation",
    "layout animation"
  ],
  "contexts": [
    "nextjs",
    "react",
    "typescript",
    "javascript"
  ],
  "title": "Add animations with Framer Motion to Next.js / React",
  "summary": "Integrate Framer Motion into a Next.js or React project — motion components, variants, page transitions, layout animations, scroll-triggered effects, and gesture-based interactions.",
  "source": "community",
  "declaration": "I will add Framer Motion animations to your Next.js or React project. This covers installing the library, basic motion components, variants for coordinated animations, page transition effects, and scroll-triggered animations. Framer Motion requires Client Components in Next.js App Router — I'll show you how to wrap the parts that need animation correctly.",
  "instructions": "## Add Animations with Framer Motion to Next.js / React\n\n### 1. Pre-flight checks\n- Confirm this is a React 18+ or Next.js 13+ project.\n- Check for any existing animation library (`react-spring`, `gsap`, `@react-spring/web`, `react-transition-group`). If found, ask the user if they want to replace it or use Framer Motion alongside it.\n- Note: In Next.js App Router, Framer Motion components must be used inside Client Components (`'use client'`). The `motion.*` components themselves are client-only.\n\n### 2. Install Framer Motion\n```bash\nnpm install framer-motion\n```\n- Framer Motion v11+ is the current major version. No separate type package needed — types are bundled.\n- Do NOT install `@types/framer-motion` — it doesn't exist and types are included in the package.\n\n### 3. Basic motion component usage\n- The simplest way to animate any HTML element is to prefix it with `motion.`:\n```tsx\n'use client'\nimport { motion } from 'framer-motion'\n\nexport function FadeInCard() {\n  return (\n    <motion.div\n      initial={{ opacity: 0, y: 20 }}\n      animate={{ opacity: 1, y: 0 }}\n      transition={{ duration: 0.4, ease: 'easeOut' }}\n      className=\"p-4 bg-white rounded-lg shadow\"\n    >\n      <p>This card fades in from below.</p>\n    </motion.div>\n  )\n}\n```\n- `initial` — the starting state (before the animation runs)\n- `animate` — the target state (where the animation ends)\n- `transition` — controls duration, easing, delay, spring config, etc.\n- Every HTML tag has a `motion.*` equivalent: `motion.div`, `motion.button`, `motion.img`, `motion.span`, etc.\n\n### 4. Variants for coordinated animations\n- Use `variants` to coordinate parent-child animations (stagger children):\n```tsx\n'use client'\nimport { motion } from 'framer-motion'\n\nconst containerVariants = {\n  hidden: { opacity: 0 },\n  visible: {\n    opacity: 1,\n    transition: {\n      staggerChildren: 0.1, // each child animates 0.1s after the previous\n    },\n  },\n}\n\nconst itemVariants = {\n  hidden: { opacity: 0, x: -20 },\n  visible: { opacity: 1, x: 0 },\n}\n\nexport function AnimatedList({ items }: { items: string[] }) {\n  return (\n    <motion.ul\n      variants={containerVariants}\n      initial=\"hidden\"\n      animate=\"visible\"\n    >\n      {items.map((item, i) => (\n        <motion.li key={i} variants={itemVariants}>\n          {item}\n        </motion.li>\n      ))}\n    </motion.ul>\n  )\n}\n```\n- Children that use `variants` inherit the parent's `initial` and `animate` values automatically — you don't need to repeat them.\n- `staggerChildren` controls the delay between each child's animation start.\n\n### 5. Page transitions in Next.js App Router\n- Create a `components/page-transition.tsx` wrapper:\n```tsx\n'use client'\nimport { motion, AnimatePresence } from 'framer-motion'\nimport { usePathname } from 'next/navigation'\nimport type { ReactNode } from 'react'\n\nexport function PageTransition({ children }: { children: ReactNode }) {\n  const pathname = usePathname()\n\n  return (\n    <AnimatePresence mode=\"wait\">\n      <motion.div\n        key={pathname}\n        initial={{ opacity: 0, y: 8 }}\n        animate={{ opacity: 1, y: 0 }}\n        exit={{ opacity: 0, y: -8 }}\n        transition={{ duration: 0.25, ease: 'easeInOut' }}\n      >\n        {children}\n      </motion.div>\n    </AnimatePresence>\n  )\n}\n```\n- Wrap the page content in `app/layout.tsx`:\n```tsx\nimport { PageTransition } from '@/components/page-transition'\n\nexport default function RootLayout({ children }: { children: React.ReactNode }) {\n  return (\n    <html lang=\"en\">\n      <body>\n        <PageTransition>{children}</PageTransition>\n      </body>\n    </html>\n  )\n}\n```\n- `AnimatePresence` handles the exit animation when a component unmounts (e.g., on navigation).\n- `mode=\"wait\"` ensures the exit animation completes before the next page enters.\n- The `key={pathname}` is critical — it tells Framer Motion this is a new element when the route changes.\n\n### 6. Scroll-triggered animations\n- Use `whileInView` for elements that animate when they enter the viewport:\n```tsx\n'use client'\nimport { motion } from 'framer-motion'\n\nexport function ScrollSection({ title, body }: { title: string; body: string }) {\n  return (\n    <motion.section\n      initial={{ opacity: 0, y: 40 }}\n      whileInView={{ opacity: 1, y: 0 }}\n      viewport={{ once: true, margin: '-100px' }}\n      transition={{ duration: 0.5, ease: 'easeOut' }}\n    >\n      <h2>{title}</h2>\n      <p>{body}</p>\n    </motion.section>\n  )\n}\n```\n- `viewport={{ once: true }}` — animate only the first time the element enters the viewport (don't re-animate on scroll back).\n- `viewport={{ margin: '-100px' }}` — triggers the animation 100px before the element fully enters the viewport.\n\n### 7. Gesture interactions (hover, tap)\n```tsx\n'use client'\nimport { motion } from 'framer-motion'\n\nexport function AnimatedButton({ children, onClick }: { children: React.ReactNode; onClick?: () => void }) {\n  return (\n    <motion.button\n      onClick={onClick}\n      whileHover={{ scale: 1.05 }}\n      whileTap={{ scale: 0.97 }}\n      transition={{ type: 'spring', stiffness: 400, damping: 17 }}\n      className=\"px-4 py-2 bg-blue-600 text-white rounded-md\"\n    >\n      {children}\n    </motion.button>\n  )\n}\n```\n- `whileHover` — applies while the mouse hovers over the element.\n- `whileTap` — applies while the element is pressed.\n- Spring transitions feel more natural for interactive elements than linear/ease transitions.\n\n### 8. Layout animations (automatically animate between states)\n```tsx\n'use client'\nimport { motion, LayoutGroup } from 'framer-motion'\nimport { useState } from 'react'\n\nexport function ExpandableCard() {\n  const [isExpanded, setIsExpanded] = useState(false)\n\n  return (\n    <motion.div\n      layout\n      onClick={() => setIsExpanded(!isExpanded)}\n      style={{ overflow: 'hidden', cursor: 'pointer', padding: 16, background: '#f0f0f0', borderRadius: 8 }}\n    >\n      <motion.h3 layout>Click to expand</motion.h3>\n      {isExpanded && (\n        <motion.p\n          initial={{ opacity: 0 }}\n          animate={{ opacity: 1 }}\n          exit={{ opacity: 0 }}\n        >\n          This content was hidden and is now revealed with a smooth layout transition.\n        </motion.p>\n      )}\n    </motion.div>\n  )\n}\n```\n- `layout` prop tells Framer Motion to automatically animate size and position changes.\n- No explicit `initial`/`animate` needed for layout changes — Framer Motion detects the layout shift and animates it.\n\n### 9. Respect user's motion preferences\n- Always respect `prefers-reduced-motion` for accessibility:\n```tsx\n'use client'\nimport { motion, useReducedMotion } from 'framer-motion'\n\nexport function AccessibleAnimation({ children }: { children: React.ReactNode }) {\n  const shouldReduceMotion = useReducedMotion()\n\n  return (\n    <motion.div\n      initial={{ opacity: 0, y: shouldReduceMotion ? 0 : 20 }}\n      animate={{ opacity: 1, y: 0 }}\n      transition={{ duration: shouldReduceMotion ? 0 : 0.4 }}\n    >\n      {children}\n    </motion.div>\n  )\n}\n```\n- `useReducedMotion()` returns `true` if the user has enabled \"Reduce Motion\" in their OS settings.\n- Always disable or reduce animations for users who have this preference.\n\n### 10. Verify the setup\n- Run `npm run dev`.\n- Navigate to a page with a `motion.*` component — it should animate on mount.\n- Open DevTools → Network and verify Framer Motion is loaded (look for `framer-motion` in the JS bundles).\n- Test `whileHover` on a button — it should scale on mouse-enter and reset on mouse-leave.\n- Test `whileInView` by scrolling — the element should animate in as it enters the viewport.\n- Enable \"Reduced Motion\" in your OS settings (System Preferences → Accessibility → Display → Reduce Motion on macOS) and verify animations are suppressed when using `useReducedMotion()`.\n\n### Common errors & fixes\n- **\"You're importing a component that needs... 'use client'\"** → A `motion.*` component is used in a Server Component. Add `'use client'` to the file or extract the animated part into a separate Client Component.\n- **Exit animations not working** → Missing `AnimatePresence` wrapper around the component that unmounts. `AnimatePresence` is required for `exit` animations.\n- **Page transitions not triggering** → The `key` prop on the `motion.div` inside `AnimatePresence` is not changing between routes. Use `usePathname()` as the key.\n- **`layout` animation causing jumps** → Other elements are shifting due to the layout change. Wrap the group with `<LayoutGroup>` to coordinate sibling layout animations.\n- **Animations run twice in React 18 Strict Mode** → React Strict Mode double-invokes effects in development. This is expected behavior — animations run once in production.\n- **TypeScript error on `motion.div` props** → Ensure `framer-motion` is installed and up to date. The types are bundled — no separate `@types` package is needed.\n- **Performance issues with many animated elements** → Use `will-change: transform` CSS on frequently animated elements, or use `transform` and `opacity` properties only (GPU-accelerated).\n\n### What NOT to do\n- Do NOT use `motion.*` components in Server Components — they require `'use client'`. Create a wrapper Client Component if needed.\n- Do NOT animate CSS properties like `width`, `height`, or `top`/`left` with raw values — prefer `transform: scale()`, `translateX()`, and `translateY()` for GPU-accelerated animations.\n- Do NOT forget `AnimatePresence` when you need exit animations — Framer Motion cannot animate an element that has been removed from the DOM without it.\n- Do NOT skip the `viewport={{ once: true }}` option on scroll animations — without it, elements re-animate every time they scroll into view, which is distracting.\n- Do NOT ignore `prefers-reduced-motion` — some users get motion sickness from animations. Always use `useReducedMotion()` for significant animations.\n- Do NOT use Framer Motion for simple CSS hover effects (color changes, underlines) — use CSS transitions for these. Reserve Framer Motion for complex, coordinated, or physics-based animations.\n- Do NOT animate layout-affecting CSS properties (`width`, `height`, `padding`, `margin`) with `animate` directly — use the `layout` prop instead, which handles layout changes more efficiently.",
  "version": "1.0.0",
  "updated_at": "2026-06-28T00:00:00Z"
}
