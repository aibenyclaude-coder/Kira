{
  "id": "community.setup-hono.v1",
  "keywords": [
    "hono",
    "setup hono",
    "add hono",
    "edge api",
    "api framework",
    "cloudflare workers",
    "cloudflare pages api",
    "edge function",
    "hono rpc",
    "lightweight api"
  ],
  "contexts": [
    "nextjs",
    "typescript",
    "nodejs",
    "cloudflare"
  ],
  "title": "Set up Hono as an API framework",
  "summary": "Install Hono, mount it in Next.js App Router or Cloudflare Workers, add Zod validation middleware, route grouping, CORS, auth, and type-safe RPC.",
  "source": "community",
  "declaration": "I will integrate Hono into this project as the API framework. This covers mounting Hono inside Next.js App Router routes, adding built-in middleware (CORS, logger, Zod validation), route groups, and optionally Hono RPC for type-safe client calls. If you need Cloudflare Workers or a standalone Node.js server instead, say so before I begin.",
  "instructions": "## Set up Hono as an API framework\n\n### 1. Install packages\n- For Next.js App Router: `npm install hono`\n- For standalone Node.js server: `npm install hono @hono/node-server`\n- For Zod validation middleware: `npm install hono @hono/zod-validator zod`\n- Hono is a single package — no extra adapters for Next.js or Cloudflare Workers.\n- Verify: `node -e \"require('hono'); console.log('ok')\"`\n\n### 2. Mount Hono in Next.js App Router\n- Create `src/app/api/[[...route]]/route.ts`:\n  ```ts\n  import { Hono } from 'hono';\n  import { handle } from 'hono/vercel';\n\n  const app = new Hono().basePath('/api');\n\n  app.get('/hello', (c) => c.json({ message: 'Hello from Hono!' }));\n\n  app.get('/users/:id', (c) => {\n    const id = c.req.param('id');\n    return c.json({ id, name: 'Alice' });\n  });\n\n  export const GET = handle(app);\n  export const POST = handle(app);\n  export const PUT = handle(app);\n  export const DELETE = handle(app);\n  ```\n- `[[...route]]` — double brackets make the catch-all optional; the segment matches `/api` itself and all sub-paths.\n- `.basePath('/api')` tells Hono the prefix Next.js adds — without it all routes 404.\n- `handle` from `hono/vercel` converts Hono's Web API Response to a Next.js-compatible response.\n\n### 3. Built-in middleware\n- Add middleware with `app.use()` before route definitions:\n  ```ts\n  import { logger } from 'hono/logger';\n  import { cors } from 'hono/cors';\n  import { prettyJSON } from 'hono/pretty-json';\n\n  app.use('*', logger());\n  app.use('/api/*', cors({ origin: ['https://yourdomain.com'] }));\n  app.use('*', prettyJSON());\n  ```\n- Available built-ins: `logger`, `cors`, `bearerAuth`, `basicAuth`, `compress`, `cache`, `etag`, `csrf`, `prettyJSON`, `secureHeaders`.\n- Middleware runs in registration order — place `logger` first, auth before protected routes.\n\n### 4. Zod input validation\n- Use `zValidator` to validate and type request bodies:\n  ```ts\n  import { zValidator } from '@hono/zod-validator';\n  import { z } from 'zod';\n\n  const createUserSchema = z.object({\n    name: z.string().min(1, 'Name is required'),\n    email: z.string().email('Invalid email'),\n  });\n\n  app.post('/users', zValidator('json', createUserSchema), async (c) => {\n    const data = c.req.valid('json'); // fully typed, validated\n    // create user...\n    return c.json({ success: true, user: data }, 201);\n  });\n  ```\n- `zValidator` automatically returns a 400 with Zod error details on invalid input — no manual try/catch.\n- The `'json'` target validates `req.json()`. Use `'query'` for query params, `'form'` for form data.\n\n### 5. Route grouping\n- Split routes into sub-routers and mount with `app.route()`:\n  ```ts\n  const users = new Hono();\n  users.get('/', (c) => c.json({ users: [] }));\n  users.post('/', zValidator('json', createUserSchema), async (c) => {\n    const data = c.req.valid('json');\n    return c.json({ created: true, user: data }, 201);\n  });\n  users.get('/:id', (c) => c.json({ id: c.req.param('id') }));\n  users.delete('/:id', (c) => c.json({ deleted: true }));\n\n  app.route('/users', users);\n  ```\n- Sub-routers do NOT inherit `.basePath()` from the parent — mount at the relative path only.\n\n### 6. Bearer auth for protected routes\n- Protect routes with `bearerAuth`:\n  ```ts\n  import { bearerAuth } from 'hono/bearer-auth';\n\n  const token = process.env.API_SECRET_TOKEN!;\n  app.use('/admin/*', bearerAuth({ token }));\n\n  app.get('/admin/stats', (c) => c.json({ users: 1000 }));\n  ```\n- For per-user auth (Clerk, Auth.js), validate the session in Next.js `middleware.ts` and pass user data to Hono via context variables.\n\n### 7. Context variables for request-scoped data\n- Pass data between middleware and handlers:\n  ```ts\n  type Variables = { userId: string };\n  const app = new Hono<{ Variables: Variables }>().basePath('/api');\n\n  app.use('*', async (c, next) => {\n    const userId = await validateSession(c.req.header('Authorization'));\n    c.set('userId', userId);\n    await next();\n  });\n\n  app.get('/profile', (c) => {\n    const userId = c.get('userId');\n    return c.json({ userId });\n  });\n  ```\n- Typing `Variables` on the Hono instance makes `c.get()` / `c.set()` fully type-safe.\n\n### 8. Hono RPC (type-safe client)\n- Export the app type for end-to-end type safety without code generation:\n  ```ts\n  // server: src/app/api/[[...route]]/route.ts\n  export type AppType = typeof app;\n\n  // client: src/lib/api-client.ts\n  import { hc } from 'hono/client';\n  import type { AppType } from '@/app/api/[[...route]]/route';\n\n  export const apiClient = hc<AppType>('http://localhost:3000');\n\n  // usage in a component:\n  const res = await apiClient.api.users.$get();\n  const data = await res.json(); // fully typed\n  ```\n- HonoRPC infers types from your route definitions — if a route return type changes, the client shows a TypeScript error.\n\n### 9. Cloudflare Workers deployment\n- Hono runs natively on Cloudflare Workers with no adapter:\n  ```ts\n  import { Hono } from 'hono';\n  const app = new Hono();\n  app.get('/', (c) => c.text('Hello Cloudflare Workers!'));\n  export default app;\n  ```\n- Deploy: `npx wrangler deploy`\n- No `handle()` wrapper needed — export the app directly as the default export.\n\n### 10. Standalone Node.js server\n- Serve with `@hono/node-server`:\n  ```ts\n  import { Hono } from 'hono';\n  import { serve } from '@hono/node-server';\n\n  const app = new Hono();\n  app.get('/', (c) => c.text('Hello Node.js!'));\n\n  serve({ fetch: app.fetch, port: 3001 }, () => {\n    console.log('Server running on :3001');\n  });\n  ```\n\n### Common errors & fixes\n- **All routes return 404** → The catch-all folder uses single brackets `[...route]` instead of `[[...route]]`. Rename the folder.\n- **`basePath` mismatch** → Your Next.js API is at `/api` but `basePath` is missing or set to `/`. Add `.basePath('/api')` to `new Hono()`.\n- **CORS errors in the browser** → `app.use('*', cors())` is missing or placed after the route definitions. Move it before all routes.\n- **`handle is not a function`** → Wrong import. Use `import { handle } from 'hono/vercel'`, not `hono/next` or `hono`.\n- **Middleware not executing** → `app.use()` is called after `app.get()` / `app.post()`. Middleware must be registered before routes.\n- **Request body is empty in POST** → `c.req.json()` is missing `await`. Always `const body = await c.req.json()`.\n- **`c.req.valid('json')` TypeScript error** → `zValidator` middleware is not applied to the route. Add `zValidator('json', schema)` as the second argument.\n\n### What NOT to do\n- Do NOT use `[...route]` (single brackets) — use `[[...route]]` to make the catch-all optional so `/api` itself is also matched.\n- Do NOT mix Next.js `export async function POST()` route handlers in the same file as a Hono mount — let Hono own all HTTP methods via `handle(app)`.\n- Do NOT skip `.basePath('/api')` when mounting inside Next.js — Hono does not automatically detect the Next.js base path.\n- Do NOT install both `hono` and `express` for the same API — pick one. Hono is the Edge-native choice; Express requires Node.js-specific adapters.\n- Do NOT use Hono for full-stack rendering or React Server Components — it is an API framework, not a full-stack framework.\n- Do NOT skip Zod validation on public endpoints — raw `await c.req.json()` without validation means any payload shape reaches your handlers.",
  "version": "1.0.0",
  "updated_at": "2026-04-20T00:00:00Z",
  "category": "backend"
}
