{
  "id": "community.setup-tanstack-router.v1",
  "keywords": [
    "tanstack router",
    "tanstack-router",
    "setup tanstack router",
    "add tanstack router",
    "file-based routing",
    "type-safe routing",
    "react router tanstack"
  ],
  "contexts": [
    "react",
    "typescript",
    "vite"
  ],
  "title": "Set up TanStack Router v1 with Vite and React",
  "summary": "Install TanStack Router, configure the Vite plugin for file-based routing, create the root and index routes, add RouterProvider, and navigate with Link and useNavigate.",
  "source": "community",
  "declaration": "I will set up TanStack Router v1 — a fully type-safe React router with first-class file-based routing, route loaders, and search parameter validation. This is for Vite + React projects. If you are using Next.js (which has its own router), say so before I begin.",
  "instructions": "## Set up TanStack Router v1 with Vite and React\n\n### 1. Pre-flight checks\n- Confirm the project uses Vite with React and TypeScript (`vite.config.ts` exists).\n- Check for conflicting routers: `npm ls react-router-dom react-router`. If found, warn the user — TanStack Router replaces React Router and they should not be used together.\n- Ensure TypeScript strict mode is enabled in `tsconfig.json`: `\"strict\": true`. TanStack Router type inference requires strict mode.\n\n### 2. Install dependencies\n```\nnpm install @tanstack/react-router\nnpm install -D @tanstack/router-plugin @tanstack/router-devtools\n```\n- `@tanstack/router-plugin`: Vite plugin that auto-generates the route tree from your file structure.\n- `@tanstack/router-devtools`: Browser devtools panel for inspecting routes, params, and search state.\n\n### 3. Configure the Vite plugin\n- Edit `vite.config.ts`:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport { TanStackRouterVite } from '@tanstack/router-plugin/vite';\n\nexport default defineConfig({\n  plugins: [\n    TanStackRouterVite(), // must come BEFORE the React plugin\n    react(),\n  ],\n});\n```\n- IMPORTANT: `TanStackRouterVite()` must be listed before `react()` in the plugins array.\n- This plugin watches `src/routes/` and auto-generates `src/routeTree.gen.ts` whenever you add, rename, or delete a route file.\n- Add `src/routeTree.gen.ts` to `.gitignore` — it is generated automatically.\n\n### 4. Create the root route (`src/routes/__root.tsx`)\n```tsx\nimport { createRootRoute, Link, Outlet } from '@tanstack/react-router';\nimport { TanStackRouterDevtools } from '@tanstack/router-devtools';\n\nexport const Route = createRootRoute({\n  component: () => (\n    <>\n      <nav>\n        <Link to=\"/\" className=\"[&.active]:font-bold\">Home</Link>\n        <Link to=\"/about\" className=\"[&.active]:font-bold\">About</Link>\n      </nav>\n      <hr />\n      <Outlet />\n      <TanStackRouterDevtools />\n    </>\n  ),\n});\n```\n- `__root.tsx` is the layout that wraps every route. `<Outlet />` renders the matched child route.\n- `Link` automatically adds the `active` class when the route is matched. Use `[&.active]:` Tailwind selector or a CSS class.\n- Remove `<TanStackRouterDevtools />` before production or wrap it with `process.env.NODE_ENV === 'development'`.\n\n### 5. Create the index route (`src/routes/index.tsx`)\n```tsx\nimport { createFileRoute } from '@tanstack/react-router';\n\nexport const Route = createFileRoute('/')({\n  component: Index,\n});\n\nfunction Index() {\n  return <h1>Hello from the index route!</h1>;\n}\n```\n- IMPORTANT: The string passed to `createFileRoute` must exactly match the file path relative to `src/routes/`. The plugin enforces this and will error if they do not match.\n\n### 6. Create additional routes\n- **Static route** (`src/routes/about.tsx`):\n```tsx\nimport { createFileRoute } from '@tanstack/react-router';\n\nexport const Route = createFileRoute('/about')({\n  component: () => <h1>About</h1>,\n});\n```\n- **Dynamic route** (`src/routes/posts/$postId.tsx`):\n```tsx\nimport { createFileRoute } from '@tanstack/react-router';\n\nexport const Route = createFileRoute('/posts/$postId')({\n  component: PostPage,\n});\n\nfunction PostPage() {\n  const { postId } = Route.useParams();\n  return <h1>Post: {postId}</h1>;\n}\n```\n- Dynamic segments start with `$`. Access them with `Route.useParams()` — fully typed.\n- **Layout route** (`src/routes/_layout.tsx`):\n  - Files prefixed with `_` are pathless layout routes — they wrap child routes without adding a URL segment.\n\n### 7. Add route loaders (data fetching)\n```tsx\nimport { createFileRoute } from '@tanstack/react-router';\n\nexport const Route = createFileRoute('/posts/$postId')({\n  loader: async ({ params }) => {\n    const post = await fetch(`/api/posts/${params.postId}`).then(r => r.json());\n    return { post };\n  },\n  component: PostPage,\n});\n\nfunction PostPage() {\n  const { post } = Route.useLoaderData();\n  return <h1>{post.title}</h1>;\n}\n```\n- Loaders run before the component renders, similar to React Router v6 loaders.\n- Data is typed automatically — `useLoaderData()` returns the exact type the loader returns.\n\n### 8. Type-safe search parameters\n```tsx\nimport { createFileRoute } from '@tanstack/react-router';\nimport { z } from 'zod';\nimport { zodValidator } from '@tanstack/zod-adapter';\n\nconst searchSchema = z.object({\n  page: z.number().int().min(1).default(1),\n  q: z.string().optional(),\n});\n\nexport const Route = createFileRoute('/search')({\n  validateSearch: zodValidator(searchSchema),\n  component: SearchPage,\n});\n\nfunction SearchPage() {\n  const { page, q } = Route.useSearch();\n  return <div>Page {page}, query: {q}</div>;\n}\n```\n- Install the Zod adapter: `npm install @tanstack/zod-adapter zod`.\n- `validateSearch` parses and validates URL search params. Invalid params cause a navigation error, not a runtime crash.\n\n### 9. Create the router and wire up the app\n- Create `src/router.ts`:\n```ts\nimport { createRouter } from '@tanstack/react-router';\nimport { routeTree } from './routeTree.gen';\n\nexport const router = createRouter({ routeTree });\n\ndeclare module '@tanstack/react-router' {\n  interface Register {\n    router: typeof router;\n  }\n}\n```\n- The `Register` interface augments TanStack Router globally, enabling type-safe `Link` `to` props and `useNavigate` targets across the whole app.\n- Update `src/main.tsx`:\n```tsx\nimport React from 'react';\nimport ReactDOM from 'react-dom/client';\nimport { RouterProvider } from '@tanstack/react-router';\nimport { router } from './router';\n\nReactDOM.createRoot(document.getElementById('root')!).render(\n  <React.StrictMode>\n    <RouterProvider router={router} />\n  </React.StrictMode>\n);\n```\n\n### 10. Navigation\n- **Link component:**\n```tsx\nimport { Link } from '@tanstack/react-router';\n\n<Link to=\"/posts/$postId\" params={{ postId: '123' }}>Go to post</Link>\n<Link to=\"/search\" search={{ page: 2, q: 'hello' }}>Page 2</Link>\n```\n- **Programmatic navigation:**\n```tsx\nimport { useNavigate } from '@tanstack/react-router';\n\nconst navigate = useNavigate();\nnavigate({ to: '/dashboard' });\nnavigate({ to: '/posts/$postId', params: { postId: post.id } });\n```\n\n### Common errors & fixes\n- **`routeTree.gen.ts not found`** → The Vite plugin did not generate the file yet. Start `npm run dev` — the file is created on first run.\n- **`Route path mismatch`** → The string in `createFileRoute('/path')` does not match the actual file location. The plugin enforces exact matches; fix the string or move the file.\n- **TypeScript errors on `Link to` prop** → The `Register` interface in `router.ts` is missing or the file is not imported. Ensure `src/router.ts` is imported in `src/main.tsx`.\n- **Loader data is `undefined`** → The component called `useLoaderData()` but the route has no `loader`. Add a loader or use `Route.useLoaderData()` only in routes that define one.\n- **`[&.active]` class not applying** → You are not using Tailwind or the selector syntax differs. Use `style={({ isActive }) => ({ fontWeight: isActive ? 'bold' : 'normal' })}` instead.\n- **`TanStackRouterVite` must come before React plugin** → If you placed the router plugin after the React plugin, HMR may not work correctly for route changes. Reorder the plugins.\n- **Search params lost on navigation** → You navigated with `navigate({ to: '...' })` without passing `search`. TanStack Router does NOT preserve search params automatically. Pass them explicitly.\n\n### What NOT to do\n- Do NOT hand-edit `src/routeTree.gen.ts` — it is auto-generated and will be overwritten on the next dev server restart.\n- Do NOT use React Router alongside TanStack Router — they conflict. Remove `react-router-dom` from dependencies if switching.\n- Do NOT skip the `Register` interface augmentation in `router.ts` — without it, `Link` `to` props are untyped strings and you lose all type safety.\n- Do NOT use `window.location.href` for navigation — use `useNavigate()` or `<Link>` to keep the router's state consistent.\n- Do NOT nest route files arbitrarily — folder structure determines URL structure. A file at `src/routes/posts/comments.tsx` creates the route `/posts/comments`.\n- Do NOT include `routeTree.gen.ts` in version control — regenerating it is instant and its content depends on your local file structure.",
  "version": "1.0.0",
  "updated_at": "2026-05-19T00:00:00Z",
  "category": "frontend"
}
