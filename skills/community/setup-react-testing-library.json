{
  "id": "community.setup-react-testing-library.v1",
  "keywords": [
    "react testing library",
    "testing library",
    "rtl",
    "component testing",
    "test react components",
    "setup testing",
    "vitest react",
    "user-event",
    "screen queries",
    "render component",
    "integration test"
  ],
  "contexts": [
    "nextjs",
    "react",
    "typescript",
    "vitest"
  ],
  "title": "Set up React Testing Library with Vitest for component tests",
  "summary": "Install @testing-library/react and @testing-library/user-event, configure jsdom for Vitest, and write component tests with screen queries, user interactions, and async state.",
  "source": "community",
  "declaration": "I will set up React Testing Library with Vitest for component testing in this project. This requires Vitest to already be configured — if it is not, I will set that up first. I will use jsdom as the browser simulation environment.",
  "instructions": "## Set up React Testing Library with Vitest\n\n### 0. Prerequisite: Vitest must be installed\n- Check `package.json` for `vitest` in `devDependencies`. If missing, set up Vitest first.\n- This guide assumes Vitest is configured with a `vitest.config.ts` or inline `vite.config.ts`.\n\n### 1. Install packages\n```bash\nnpm install -D @testing-library/react @testing-library/user-event @testing-library/jest-dom jsdom\n```\n- `@testing-library/react` — `render()`, `screen`, and query utilities\n- `@testing-library/user-event` — realistic user interaction simulation (types, clicks, selects)\n- `@testing-library/jest-dom` — custom matchers like `toBeInTheDocument()`, `toHaveValue()`\n- `jsdom` — browser DOM simulation for Node.js\n- Do NOT install `@testing-library/dom` separately — it is a peer dependency installed automatically.\n\n### 2. Configure Vitest for jsdom (`vitest.config.ts`)\n```ts\nimport { defineConfig } from 'vitest/config';\nimport react from '@vitejs/plugin-react';\n\nexport default defineConfig({\n  plugins: [react()],\n  test: {\n    environment: 'jsdom',\n    globals: true,\n    setupFiles: ['./src/test/setup.ts'],\n  },\n});\n```\n- `environment: 'jsdom'` enables the browser DOM simulation.\n- `globals: true` makes `describe`, `it`, `expect`, `beforeEach` available without imports.\n- `setupFiles` runs before each test file — used to import jest-dom matchers.\n\nIf using `vite.config.ts` (no separate vitest config), add the `test` block there:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\n\nexport default defineConfig({\n  plugins: [react()],\n  test: {\n    environment: 'jsdom',\n    globals: true,\n    setupFiles: ['./src/test/setup.ts'],\n  },\n});\n```\n\n### 3. Create the setup file (`src/test/setup.ts`)\n```ts\nimport '@testing-library/jest-dom';\n```\n- This single import extends Vitest's `expect` with jest-dom matchers globally.\n- Without this, `toBeInTheDocument()` and similar matchers will throw `TypeError`.\n\nAdd TypeScript declaration for the matchers in `src/test/setup.ts` or in a `vitest.d.ts` file:\n```ts\nimport type { TestingLibraryMatchers } from '@testing-library/jest-dom/matchers';\n\ndeclare module 'vitest' {\n  interface Matchers<R>\n    extends TestingLibraryMatchers<typeof expect.stringContaining, R> {}\n}\n```\n\n### 4. Write your first component test\n\nExample component (`src/components/Greeting.tsx`):\n```tsx\nexport function Greeting({ name }: { name: string }) {\n  return <h1>Hello, {name}!</h1>;\n}\n```\n\nTest file (`src/components/Greeting.test.tsx`):\n```tsx\nimport { render, screen } from '@testing-library/react';\nimport { Greeting } from './Greeting';\n\ndescribe('Greeting', () => {\n  it('renders the name', () => {\n    render(<Greeting name=\"Kira\" />);\n    expect(screen.getByRole('heading', { name: /hello, kira/i })).toBeInTheDocument();\n  });\n});\n```\n- Always query by role (`getByRole`) when possible — it matches how assistive technology sees the page.\n- Use case-insensitive regex (`/text/i`) to avoid brittle string matching.\n\n### 5. Test user interactions with `userEvent`\n```tsx\nimport { render, screen } from '@testing-library/react';\nimport userEvent from '@testing-library/user-event';\nimport { SearchForm } from './SearchForm';\n\ndescribe('SearchForm', () => {\n  it('calls onSearch with the input value when submitted', async () => {\n    const user = userEvent.setup(); // create a user session\n    const onSearch = vi.fn();\n\n    render(<SearchForm onSearch={onSearch} />);\n\n    await user.type(screen.getByRole('textbox', { name: /search/i }), 'hello world');\n    await user.click(screen.getByRole('button', { name: /search/i }));\n\n    expect(onSearch).toHaveBeenCalledWith('hello world');\n  });\n});\n```\n- Always use `userEvent.setup()` at the start of each test — it creates a browser-like event queue.\n- All `user.*` methods are `async` — always `await` them.\n- Do NOT use `fireEvent` for user interactions — it fires low-level DOM events without browser behavior. Use `userEvent` instead.\n\n### 6. Test async state (loading, data fetching)\n```tsx\nimport { render, screen, waitFor } from '@testing-library/react';\nimport { vi } from 'vitest';\nimport { UserProfile } from './UserProfile';\n\nvi.mock('@/lib/api', () => ({\n  getUser: vi.fn().mockResolvedValue({ id: '1', name: 'Alice' }),\n}));\n\ndescribe('UserProfile', () => {\n  it('shows user name after loading', async () => {\n    render(<UserProfile userId=\"1\" />);\n\n    expect(screen.getByText(/loading/i)).toBeInTheDocument();\n\n    await waitFor(() => {\n      expect(screen.getByText('Alice')).toBeInTheDocument();\n    });\n\n    expect(screen.queryByText(/loading/i)).not.toBeInTheDocument();\n  });\n});\n```\n- `waitFor()` retries the assertion until it passes or times out (default 1000ms).\n- `queryByText` (not `getByText`) is used for \"should NOT be present\" assertions — `getBy` throws if not found.\n\n### 7. Test with context providers\n```tsx\nimport { render, screen } from '@testing-library/react';\nimport { QueryClient, QueryClientProvider } from '@tanstack/react-query';\nimport { UserCard } from './UserCard';\n\nfunction renderWithProviders(ui: React.ReactElement) {\n  const queryClient = new QueryClient({\n    defaultOptions: { queries: { retry: false } }, // disable retries in tests\n  });\n  return render(\n    <QueryClientProvider client={queryClient}>\n      {ui}\n    </QueryClientProvider>\n  );\n}\n\ndescribe('UserCard', () => {\n  it('renders user data', async () => {\n    renderWithProviders(<UserCard userId=\"1\" />);\n    expect(await screen.findByText('Alice')).toBeInTheDocument();\n  });\n});\n```\n- Create a reusable `renderWithProviders` helper in `src/test/utils.tsx` and import it in all component tests.\n- `screen.findByText()` is the async version of `getByText` — it waits for the element to appear.\n\n### 8. Run the tests\n```bash\n# Run once\nnpx vitest run\n\n# Watch mode\nnpx vitest\n\n# With coverage\nnpx vitest run --coverage\n```\n\n### Common errors & fixes\n- **`toBeInTheDocument is not a function`** → `@testing-library/jest-dom` is not imported in the setup file. Check `src/test/setup.ts` imports `@testing-library/jest-dom` and that `setupFiles` points to it in `vitest.config.ts`.\n- **`document is not defined`** → `environment: 'jsdom'` is missing in `vitest.config.ts`. Add it to the `test` config.\n- **`Cannot find module 'react'`** → The Vite React plugin is missing. Install `@vitejs/plugin-react` and add it to the `plugins` array.\n- **`act(...)` warning in console** → A state update happened after the test ended. Wrap async interactions in `await waitFor()` or use `await user.click()` / `await user.type()` properly.\n- **`Unable to find role 'button'`** → The element has a different role or the `name` option doesn't match the accessible label. Use the Testing Library browser extension or `screen.debug()` to inspect the rendered output.\n- **Flaky tests due to timing** → Using `setTimeout` or raw Promises instead of `waitFor`. Always use `waitFor` or `findBy*` queries for async assertions.\n- **`vi.mock` not hoisted** → `vi.mock()` calls must be at the top level of the test file (not inside `describe` or `it`). Vitest hoists them automatically only at the module top level.\n\n### What NOT to do\n- Do NOT use `fireEvent.click()` for button clicks — use `await user.click()` from `userEvent` for realistic browser events.\n- Do NOT use `getByTestId` as the primary query — prefer `getByRole`, `getByLabelText`, or `getByText`. `data-testid` should be a last resort.\n- Do NOT query by CSS class names or element tags like `container.querySelector('.btn')` — tests based on implementation details break on refactors.\n- Do NOT share a single `QueryClient` instance across tests — create a new one per test to prevent state bleed.\n- Do NOT import from `@testing-library/react` and `@testing-library/dom` in the same file — always import `screen`, `render`, and `waitFor` from `@testing-library/react`.\n- Do NOT test implementation details (internal state, private methods) — test behavior from the user's perspective.\n- Do NOT skip `await` on `userEvent` calls — they are async and skipping `await` causes race conditions.",
  "version": "1.0.0",
  "updated_at": "2026-06-06T00:00:00Z"
}
